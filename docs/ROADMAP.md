# AutoChessBossRush 開発ロードマップ

> Codex から引き継いだプロトタイプを、ローグライク・オートチェスとして完成させるための計画。
> このファイルは Claude と共有する生きた設計メモ。自由に編集・追記してOK。

## ビジョン

チャプター制で進行するローグライク・オートチェス（ボスラッシュ）。
序盤はコスト3以下のユニットしか出ず、ボス戦やイベントラウンドでコスト4ユニット・アイテム・オーグメントを集めながら強化していき、最終局面でコスト5ユニットに挑む。
チャプターごとにクリアタイム/スコアを記録してランキング化する。

---

## エピック（実装フェーズ）

### E1. チャプター & ラウンド構造（基盤）
- 全ラウンドを ~30 に拡張し、チャプターに分割（提案: 3章 × 約10ラウンド）。
- ラウンド種別を導入: 通常戦 / ボス戦 / イベント（アイテム選択・ユニット獲得・オーグメント選択・ショップ強化など）。
- チャプターごとの最終ボス（章1=◯◯, 章2=△△ …）。
- ショップのコスト解放ゲート: 序盤コスト3まで → ボス/イベントでコスト4解放 → 終盤コスト5。
- **依存: 現状の flat な waveDefinitions を一般化。多くの機能の土台。**

### E2. 経済: 利子（TFT式）
- 所持金に応じた利子（10%ごと、上限 +5/ラウンド 等）、勝敗ゴールド、連勝/連敗ボーナス。
- お金管理の駆け引きを体験させる。比較的独立して実装可能。

### E3. オーグメント（TFT式）
- 特定ラウンドで3択提示。レアリティ3段階（シルバー/ゴールド/プリズム等）。
- 効果適用フック（戦闘開始・経済・シナジー・特定ユニット強化など）。中〜大規模の新システム。

### E4. スコアリング & ライブ表示 & 記録/ランキング
- スコア要素: スターアップ、アイテム不使用でのウェーブ突破、クリアタイム、連勝、ノーデス 等。
- プレイ中に「スコア加算の瞬間」をポップアップで可視化。
- チャプターごとにタイム/スコアを記録 → ランキング表示。
- **依存: E1。永続化レイヤーが新規に必要（当初スコープ外だったセーブが、ランキングのため必須化）。まずローカル保存を想定。**

### E5. スキル / シナジー説明の透明化
- スキル説明にダメージ量・回復量・シールド量の実数値を明記（未記載のものを洗い出す）。
- シナジー説明も同様に数値を明記。
- できればステータス連動の数値から説明文を自動生成し、数値とテキストがズレないようにする。

### E6. 演出強化（VFX / SFX）
- コスト5未満、特に **コスト4ボスユニット** のスキル演出を満足度の高い迫力ある演出に。
- シナジー発動の演出強化。
- AttackEffectPlayer の拡張。反復的・最後の磨き込みフェーズ向き。

---

## 主要な決定事項（2026-05-28 確定）
- **チャプター構成: 1チャプター = 30ラウンドの長期戦**。複数チャプターを用意し、章ごとに最終ボスを変える（章1=◯◯, 章2=△△ …）。長く遊べることを重視。
- **ランキング: オンラインも視野に**。まずローカル保存で実装しつつ、永続化層を抽象化して後からバックエンド（リーダーボード）へ差し替え可能な設計にする。
- 着手順: **E5（説明の透明化）を最初に**着手。以降は順次相談。
- オーグメントの規模・点数は E3 着手時に決定。

## 進行メモ
- 2026-05-27: wave 6 → 12 に暫定拡張済み。E1 のチャプター枠組みへ統合予定（1章=30ラウンドへ再編）。
- 2026-05-28: **E5 完了** — 固有スキル全28体の説明を実数値化（UnitStatusPanelUI に専用ビルダー追加）、シナジー proc の実値もツールチップへ明記（SynergyTooltipUI）。
- 2026-05-28: **E2 経済ループ実装** — ウェーブクリア収入＝基本5＋利子（10ごと+1, 上限5）＋連勝ボーナス。所持金デバッグ999→初期4（PlayerData.startingMoney で調整可）。※収入内訳のUI表示は E4 のライブ表示で実装予定。
- 2026-05-28: フィードバック反映 — 開始時にランダムなコスト1ユニットを1体付与（手詰まり防止 / GameManager.GrantStartingUnit）、PvEのため**連勝ボーナスを撤廃**（収入＝基本＋利子のみ）。
- 2026-05-28: **E1 着手①** — 進行連動のショップ・コスト解放ゲートを実装。`GameManager.MaxAvailableShopCost`（初期3、ボス撃破ごと +1、上限5）で UIShop の抽選コストを制限。`UnlockNextShopCostTier()` はイベントからも呼べる。
- 2026-05-28: **E1 着手②** — ラウンド種別を導入。`WaveDefinition` を「ラウンド定義」に一般化し、戦闘なしの**イベントラウンド**（`WaveEventType`: BonusItem / BonusGold）を追加。`TryStartEventRound()` が前ウェーブクリア後・ボス報酬選択後・開始時・FIGHT時に自動消化し、報酬を渡して次ラウンドへ進める。デモとして3イベント（ボス前アイテム/中盤ゴールド/最終ボス前アイテム）を挿入。`UnlockNextShopCostTier()` をイベントから呼べばコスト解放イベントも作れる。
- 2026-05-28: **E1 着手③（核を完成）** — チャプター制を導入し、**チャプター1=全30ラウンド**を構築（通常19・イベント8・ボス3＝R10/R20/R30）。`InitializeWaveDefinitions`→`BuildChapterRounds(chapter)`→`BuildChapter1Rounds()` でチャプター追加が容易な構造に（`currentChapter` フィールド）。コスト解放：ボス1→4、ボス2→5。ボス報酬は**解放済みを除外**するよう改善し、3ボスで報酬3体（Snowchasermk/Solfist/Maehvmk）を順に集められる。
- 2026-05-28: **雑魚機能 P1-P4 完了** — (P1) reference/duelyst の neutral_zyx.png を Assets にコピー＆plistから97フレームをスライス（Y反転）。(P2) `Assets/Prefabs/Unit/Neutral/Zyx.prefab` 作成（HP30/dmg4/range4/atkSpd0.6, maxMana9999で詠唱無効, idle静止スプライト）。Entity DatabaseにZyx(cost=0)を登録、`IsLegionOnlySummonData` にZyxを追加してショップ・召喚から除外。(P3) `EnemyDrop` 構造体＋`WaveEnemyPlacement.DropCoins/DropItem` を追加。SpawnWaveEnemy で登録、UnitDead で付与。(P4) チャプター1を 30→**33ラウンド・4ステージ構造**に刷新: Stage1=雑魚3 / Stage2-4=各10。中ボス2-5,2-10,3-5,3-10,4-7,4-8,4-9（撃破でコスト上限+1）、章ボス4-10（報酬選択）。WaveDefinition に `StageIndex`/`RoundInStage`/`IsMidBossWave` 追加。
- 2026-05-28: **雑魚機能 P5 完了** — `RoundProgressUI` をステージ単位表示に刷新。`SetStageProgress(stage, roundInStage, kinds, ...)` に切替。タイトルは「ラウンド/中ボス/章ボス/イベント X-Y」表記。**ステージ切替時にDOTweenでスライド+フェード遷移**（左に流す→中身差替→右からスライドイン、タイトルも軽く弾ませる）。アイコンは Combat=丸/Event=四角/Boss・MidBoss=ひし形、種別ごとに色分け。
- 2026-05-29: **E3 オーグメント完全実装** — 残課題だった複雑系も着地。
  - **召喚体系3種**: `gold_summon_dmg`（+30% 与ダメ）/`prism_summon_master`（+30% HP + 召喚体+1）/`gold_elite_summon`（戦闘開始時にランダムなコスト3を一時加勢）。`GameManager.ApplyAugmentSummonBonuses` で `SpawnTemporarySummonFromSynergy`/`SpawnTemporarySummonByUnitName`/`SpawnAugmentEliteSummon` に共通注入し、`BaseEntity.ApplyAugmentSummonStatMultipliers` で `originalBaseHealth`/`originalBaseDamage` を一括拡張。
  - **`prism_all_synergy`**: `SynergyManager.CountSynergiesForTeam` でプレイヤーチーム時のみ、各ユニットの所持シナジーを 2 倍カウント。
  - **`prism_warrior_kill_buff`**: 戦士の撃破者を UnitId キーで蓄積（`warriorKillBuffPendingByUnitId`）。`Die()` から `NotifyEnemyKilledByPlayer(killed, killer)` でキラーを伝搬。次戦闘開始時に `ApplyPrismWarriorKillBuffAtBattleStart` がキル数×30%（上限300%）の時限与ダメバフを各戦士へ付与。
  - **ベンチ拡張3種** (`silver/gold/prism_bench_*`): `EffectiveBenchSlotCount = benchSlotCount + BenchSlotBonus` 化。`HasBenchSpace`/`GetFreeBenchSlot`/`CanPlaceEntityOnBench`/`GetBenchSlotAtWorldPosition`/`GetBenchTileAtSlot` を実効値で参照。`EnsureExtraBenchTiles` が既存タイル間隔を推定して左右ベンチへ動的に追加生成。
- 2026-05-29: **オーグメント可視化パス完了** — 効果を「数字や HUD で見える化」する一斉対応。
  - **AugmentHudUI**: 画面右上に獲得済みオーグメントをティアカラー（Silver=銀/Gold=金/Prism=紫）で並べる常時表示の HUD。ホバー/クリックで AugmentTooltipUI が説明を表示。Reference duelyst の `collection_card_rarity_*` を `Assets/Resources/UI/Augment/rarity_*` にコピーして使用。
  - **AugmentTooltipUI**: ティアバー付きの説明ポップアップ（reference の `card_background` を panel sprite に流用）。
  - **リロールボタン**: `RefreshRerollButtonCostText()` が augment 状況に応じて数字を「2/1/0/無料/FREE」に動的書き換え。`NormalizeShopTextLayout` が "FREE"/"無料" を cost text と判定するよう更新し、ラベル化されないようにケア。
  - **無料リロールのスタック化**: `gold_free_reroll` を消費しなかった場合、ラウンドクリア時に `goldFreeRerollStacks++`。リロールボタン右上に reference の `icon_cooldown_counter`（`badge_counter`）でストック数バッジ `x N` を表示。消費順は「prism 永続 → スタック → 今ラウンドの gold_free_reroll」。
  - **シナジーパネル**: 各シナジー行に「+N」バッジを追加（emblem 系 + 戦闘中ランダム上乗せ分）。`prism_all_synergy` が有効なときはヘッダーに「★ 全シナジー +1 重ね掛け」を紫表示。
  - **デバッグアイテム配布の停止**: シーン上 `GameManager.spawnDebugItemsOnStart` を `False` に保存 → ゲーム開始時のアイテムベンチは空。
- 2026-05-29: **ビジョン確定 & リリース計画策定**（Claude）。プラットフォーム＝**Steam 買い切り**、ソーシャル＝**ランキング中心の軽い繋がり**、形態＝**アドベンチャー進行型（数十章＋ボス仲間化メタ進行＋東方風ボス掛け合い）**を正面から作ると確定。市場調査と差別化方針は [MARKET_POSITIONING.m
- 2026-05-30: **アルカナ(cost5)スキルモーション改修**（Claude/Cowork+Unity MCP）。不格好だった発動演出を修正。`BaseEntity.AttackSequenceCoroutine` にアルカナ専用分岐を追加し、`ArcanaChargedSkillCoroutine`＝「Ability を最後まで全再生 → `ArcanaHoldChargeCoroutine` で終盤コマ(0.9〜1.0)を約0.4秒ループ＝溜め → 魔法陣と共にダメージ」の順に。`ExecuteArcanaTomeOfFinality` から `special` トリガと他キャラ使い回しの `PlaySynergyEffect(Abyss/Arcanist)` を撤去し、アルカナ固有の **「血の魔法陣」** に差し替え。`AttackEffectPlayer.PlayArcanaBloodCircle/PlayArcanaBloodSplatter` を新設し、**これまで未使用だった赤系素材**（`fx_circle_01` 赤リング行5 / `fx_circle_00` 赤封印行1 / `fx_impactred` の big＝血しぶき / `fx_redlightning`）を合成。Compilation OK（Errors 0）。外部の無料「血の魔法陣」素材候補は別途提示予定（差し替え/追加用）。d](MARKET_POSITIONING.md)、リリース工程と task-id は [RELEASE_PLAN.md](RELEASE_PLAN.md) に集約。
- 2026-05-29: **クリティカルパス設計書3本を起票**（Claude）。実装順は R1-persist → R1-meta → R1-score。
  - [DESIGN_R1-persist.md](DESIGN_R1-persist.md) — 永続化層（ISaveStore/LocalJsonSaveStore/SaveManager）。章進捗・所持ボス仲間・ベストスコアを保存、Steam差し替え可能に。**最初に実装**。
  - [DESIGN_R1-meta.md](DESIGN_R1-meta.md) — ボス仲間化の永続roster＋章前編成UI。既存 `SelectBossReward`/`unlockedBossRewardUnitIds`（ラン内）を永続へ拡張。**差別化の核**。
  - [DESIGN_R1-score.md](DESIGN_R1-score.md) — 既存 `QueueStageResult` を明文化＋ベスト保存＋ライブ加点ポップアップ＋リザルトのベスト併記。
  - 注: 「複数チャプター」は単なる量産でなく、**ボス仲間化メタ進行＋物語**として再定義（RELEASE_PLAN の R1-meta / R2-chapters）。
- 2026-05-30: **R1 クリティカルパス3本を実装**（Claude）。Save 抽象化 → 章ベスト保存＋ポップアップ → ボス仲間 roster までを一気通貫で繋いだ。
  - `Assets/Scripts/Save/`（`ISaveStore` / `LocalJsonSaveStore` / `SaveManager` / `SaveData`）追加。`Application.persistentDataPath/save.json` に原子的書き込み、破損時は `save.corrupt.json` へ退避。`SaveManager.EnsureExists()` を `GameManager.Start` の最上段で起動。
  - `ScorePopupUI` 新規。`TrackStageProgress` 内で戦闘/中ボス/章ボスのクリア時に「+100 / +300 / +1000」を即時フロート表示（JA/EN、stacking）。
  - `QueueStageResult` の `isChapterClear` 分岐で `SaveManager.RecordChapterResult(currentChapter, totalScore, totalTime, true)` を呼び、戻り値の `isNewRecord` と `previousBest` を `pendingResult*` 経由で `ResultPanelUI.ShowStageResult` へ伝搬。`ResultPanelUI` を再レイアウト（panel 高さ 520→580）し、章クリア時のみベスト行と「★ NEW RECORD!」バッジを表示。
  - `GameManager.ChapterBossUnitIds`（chapter→unitId map、章1=`Legion`）を導入し、章クリアで `SaveManager.AddBossAlly` を呼ぶように。
  - `ChapterRosterUI` 新規（`BossRewardSelectionUI` ベースに「連れて行かない」スキップボタンを追加）。`GameManager.Start` で `TryShowChapterRoster` を `GrantStartingUnit` 直後に呼び、`BossAllies` 非空のときのみ展開→選択時 `CreateBenchEntity(data, 1)` でベンチに★1配置。
  - 永続化テスト観点: 章1クリア→再起動で `Legion` が roster に残り、開始時に選択するとベンチに加わる。ベスト/タイムも保持。
- 2026-05-30: **R1 実装レビュー完了 ✅ 承認**（Claude）。3設計書に Review 節を追記。コードレビュー上、各受け入れ基準を充足。メタ進行ループ（章クリア→`AddBossAlly("Legion")`→編成画面に出現）は `Legion` が DB に prefab 付き cost5 で存在し除外対象でないことを確認して成立を検証。**残: ①実機で「章1クリア→再起動→Legion編成→開始」を1周通す動作確認、②R3-balance で cost5 ボス仲間の初期★/ステータス減衰を調整（強すぎ懸念）。**
- 2026-05-30: **アルカナ調整＆分担作業**（Claude=Cowork+MCP / Claude Code=純ロジック）。
  - **#1 左右反転（Cowork）**: 7シートをセル単位で水平反転し「右向き基準」に統一→既存のチーム向き判定で味方=右/敵=左が自動。実機確認済み。
  - **#2 モーションキャンセル緩和（Cowork）**: 原因＝36フレーム長クリップが攻撃間隔に詰められ早送り。Attack/Ability クリップを 24fps 化して尺を短縮。残りはプレイテストで微調整。
  - **#3 3回スキル→終焉の書(Cowork)**: SpecialMove(Charge+Activate)を72フレーム/24fpsの `Arcana_Special` クリップ化、Animator に Special 状態＋"special" トリガー追加。`BaseEntity.ExecuteArcanaTomeOfFinality` を改修＝`arcanaSkillCastCount` で3回ごとに必殺（強化版・波数/威力増＋special発動）、通常は弱版。Compilation OK。必殺モーション実機確認済み。
  - **#4 デバッグ機能 / #5 シナジー死亡カウント維持 → Claude Code へ引き継ぎ**（[DESIGN_handoff_claudecode.md](DESIGN_handoff_claudecode.md)）。別ファイル/SynergyManager中心で BaseEntity と衝突しない分担。
- 2026-05-30: **アルカナ Unity 統合 完了**（Claude, unity-synaptic MCP 経由で直接実装）。
  - ツール生成の7モーションシートを共通スケール・共通アンカーで再パック（[DESIGN_arcana_integration.md](DESIGN_arcana_integration.md)）→ 元7枚を整列版で置換、各 6×6=36コマ・340×340・中央ピボットでスライス（計252スプライト）。**モーション遷移で本体がズレない**ことを実機レンダリングで検証。
  - クリップ5本（Default/Move←DefaultAndMove, Attack, Ability=終焉の書, Dead）＋ `Assets/Animations/Arcana/Arcana.controller`（Legion流の5ステート）。`Assets/Prefabs/Unit/T5/Arcana.prefab`（Maehvmk雛形）。**サイズ/位置微調整: localScale 0.55（T4ボスの約1.3倍＝最終ボスらしい最大体格）＋スプライトpivot(0.5,0.40)で浮遊（足を他ボスの足元付近まで上げる）**。スライスのpivotも全7枚 alignment=Custom (0.5,0.40) に統一（遷移ズレ無し）。
  - Entity Database に Arcana 登録（cost5 / Apex・Abyss・Arcanist、icon/frame 設定）。**ショップ出現確認済み**: `IsEntityUnlockedForShop`→true（LegionOnly除外でもボス報酬ゲートでもない）。cost5解放後に通常抽選で出現・購入可能。
  - `BaseEntity`: `GetConfiguredBaseRange` に Arcana=range5、固有スキル「終焉の書」`ExecuteArcanaTomeOfFinality`（全敵に深淵魔弾を★連動 2/3/4 波＋Arcanistマナ回収）。`UnitStatusPanelUI` に名称/実数値説明（JA/EN）。**Compilation OK（アセンブリ再読込で新メソッド確認）**。
  - 残: Enhance(SkillCharge)/SpecialMove(Aura)シートはスライス済みだが未配線（将来 VFX/演出で活用）。プレイテストで火力・サイズ・浮遊オフセットの微調整（R3-balance）。
- 2026-05-30: **最終ボス「アルカナ」仕様**（Claude）。[DESIGN_boss_arcana.md](DESIGN_boss_arcana.md) を起票。118フレームのアニメSpriteSheetは生成不可のため、作画バイブル＋2048×2048/130×130の正確なフレーム座標＋Unity取込(plist/Animator)＋EntityDB(cost5/Apex・Abyss・Arcanist/range5)＋固有スキル「終焉の書」を仕様化。art調達方法（発注/専用ツール/duelystリカラー）が未決。
- 2026-05-30: **スキル刷新 設計**（Claude）。汎用9種を共有する17体（全コスト1〜3）に固有スキルを新設する [DESIGN_skill_overhaul.md](DESIGN_skill_overhaul.md) を起票。E5 の流儀（実数値・シナジー連動・スター倍率）に合わせ、被りなしの17スキルを定義。遠距離監査の結論=**固有28体は射程を正しく活用済み（ミスマッチ無し）**、唯一の注意は汎用バフオーラがキャスター中心という設計ガイドラインのみ。実装3点（`TryExecuteDedicatedSkill`追加 / `IsXxxSkillUnit`から除外 / `UnitStatusPanelUI`固有文追加）＋新汎用デバフ（Vulnerable/Weaken/Burn/on-hit毒）。3バッチ実装を推奨。
- E1 残（ポリッシュ）: ①複数チャプター、②イベントの**選択UI化**。雑魚機能 P6（難易度調整）はプレイテスト後の反復作業。
  - ~~③旧定性スキル文フォールバックの剪定~~ → **完了済み（2026-05-29 A-3 refactor `204c7074`）**。確認の結果、UnitStatusPanelUI に独立した死にコードの定性文は無く、`BuildGenericSkillText` は実数値ジェネレータ（dedicated → generic → AppendExtra の正規パス）。全Build*メソッドは参照あり。下記の残課題メモは陳腐化のため撤回。
- 残課題メモ（2026-05-30 更新）: スキル刷新で17体に固有文が付いた結果、汎用文 `BuildGenericSkillText` に到達するのは事実上 **Zyx（cost0中立雑魚, maxMana9999で詠唱無効）のみ**。死にコードではないが、Zyx のパネルに「次の攻撃でNダメージ」と詠唱しないスキル文が出るのは軽微な誤誘導。**任意ポリッシュ**: スキルを持たない雑魚は skill 行を「—」/非表示にする（UnitStatusPanelUI 編集が必要なため、Cowork の Arcana 作業コミット後に着手するのが安全）。
- 2026-05-30: **Arcana BloodMage必殺＋共通戦闘挙動＋SE増量**（Claude/Cowork+Unity MCP）。
  - **共通**: `MeleeEntity/RangedEntity.Update` に「攻撃モーション中(`IsAttackMotionPlaying`)は移動・再ターゲットしない」分岐を追加。攻撃中に敵が射程外へ離れてもその攻撃は命中（コルーチンが `targetAtCast` を保持）。攻撃→必ずモーション完了→Moveの順に。
  - **Arcana サイズ**: prefab localScale 0.55→0.275（半分）。
  - **Arcana スキル全面改修**: 詠唱周期を4回に。1〜3回目=攻撃対象位置に持続 **BloodMage**（start→loop固定・半径2マスの弱DoT[≈12%/s]＋ゆっくり吸引、戦闘終了まで残存）。4回目=既存BloodMageを end で消し、`SpecialMove(special)` 発動＋スプライト発光＋**画面明転(白)＋BGMダック→復帰**＋盤面全体BloodMageで全敵を吸引し3.5秒大DoT＋画面シェイク。★1/2は即死させず大ダメのみ（反撃余地）、★3は吸込時(≈1秒)に全敵消滅。新規 `BloodMageVisual.cs`、`AttackEffectPlayer.SpawnBloodMage/EndBloodMage/PlayArcanaSpriteGlow/FlashScreen/FadeBgm/PlayCue` を追加。
  - **SE増量**: SFX名(670個)がユニット名と不一致のため、**SkillTheme(属性)アーキタイプ別＋主要ユニット固有**で通常攻撃の振り/着弾/死亡SEを結線（`PlayUnitAttackSwing/PlayUnitAttackImpact/PlayUnitDeathSfx`、faction f1〜f6 generalセット＋spell音）。着弾に属性色スパークVFXも追加。`PlayAttack`/`PlayDeath` を差し替え、`CompleteNormalAttackHit` に着弾SE+VFXを追加。Compilation OK（Errors 0）。動作確認はエディタで後日まとめて。
- 2026-05-30: **Arcana追補（動作確認フィードバック反映）**（Claude/Cowork+Unity MCP）。
  - **Special時サイズ補正**: SpecialMove_Activateは魔法陣リング込みで本体が一回り小さく見えるため、フィナーレのSpecial再生中だけ本体localScaleを×1.5（`ArcanaSpecialScaleCompensation`）して他モーションと揃える。終了で等倍へ復帰。
  - **スキル説明文更新**: `UnitStatusPanelUI.BuildArcanaSkillText` を新BloodMage仕様（1〜3回目=持続BloodMage吸引+弱DoT、4回目=明転+盤面全体BloodMageで大ダメ、★3全滅）にJA/EN刷新。
  - **通常攻撃SE**: 重い近接音→魔弾向けの軽い魔法詠唱/着弾音（dreamgazer/astralcrusader/spell_truestrike）へ変更。
  - **Arcana専用シナジー「終焉」(Finality)新設**: `SynergyType.Finality` 追加。SynergyManager(ResolveTier/GetNextRequiredCount/GetTierSummary/CountedSynergies)・LocalizationManager(終焉/Finality)・SynergyIconLibrary(深紅色/FIN/abyssアイコン流用)を結線。Arcanaプレハブ synergy1=19 に割当。効果=盤面に残るBloodMage1体ごとにArcanaのスキル威力&マナ回収+15%（`ArcanaFinalityBonus`）。1体で常時1/1発動の固有パッシブ。Compilation OK（Errors 0）。
- 2026-05-30: **ツールチップ/コイン内訳UI改善＋売却SE差し替え**（Claude Code, 純UI・Cowork非干渉ファイル）。
  - **パネル位置固定化**: シナジー/アイテムのツールチップをカーソル追従→**左上（シナジーパネル右隣）に固定表示**。共有定数 `TooltipLayout`（X=170, 上余白70）を新設し SynergyTooltipUI/ItemTooltipUI の `MoveNearPointer` を `MoveToFixedPosition` へ置換（旧 `pointerOffset` 撤去）。
  - **パネル外クリックで閉じる**: 各 `Update` に左クリック判定（`RectTransformUtility.RectangleContainsScreenPoint` でパネル外のみ Hide）を追加。右クリック/Escの従来挙動は維持。3パネルは相互排他（開く時に他をHide）。
  - **コイン内訳パネル新設**: `CoinIncomePanelUI`（所持金/基本収入/利子(現在・上限)/次回収入＋利子ゲージ、JA/EN・ライブ更新）。コイン表示テキストをクリックで開閉（`CoinDisplayClickProxy`＋`AttachToCoinDisplay`、UIShop.Refresh から結線）。ショップ下の小型ゲージは併存のまま。
  - **売却SE**: `Draggable.TrySellToShop` の `PlayUiSfx("unit_sell")`→`PlayUiSfx("Sell")`（AttackEffectPlayer の default解決で `Assets/Resources/sfx/Sell.mp3`、Cowork管理ファイル非編集）。
  - ※Unity でのコンパイル確認は Cowork 側で実施依頼（Arcana 作業と同時編集を避けるため当方からは Editor 再コンパイルを起動せず）。
- 2026-05-31: **Arcana追補2（サイズ/当たり判定の手直し）**（Claude/Cowork+Unity MCP）。
  - **Special段階別サイズ**: Arcana_SpecialはCharge(前半1.5s)→Activate(後半1.5s)の2段と判明。前回の一律×1.5でCharge(本体が元々大きい)が大きく見えていたため、Charge=等倍／Activate(リング込みで小さい)=×1.5 に分離（境界=クリップ50%で切替、終了で等倍）。
  - **当たり判定**: localScale0.275でBoxCollider2Dが1×1だと実寸0.275で小さすぎ、選択/ドラッグ/アイテム装着が困難だった。Collider m_Sizeを3.6×3.6（実寸≈0.99＝通常ユニット相当）に拡大。Compilation OK（Errors 0）。
- 2026-05-31: **Arcana追補3（フィナーレ演出の磨き込み＋カウントリセット）**（Claude/Cowork+Unity MCP）。
  - **スキル回数のラウンドリセット**: `ResetBattleTemporaryState`に`ResetArcanaCombatState`追加。`arcanaSkillCastCount=0`／持続BloodMage片付け／finaleコルーチン停止／スケール・速度を正規へ。次ラウンドへ詠唱回数を持ち越さない。
  - **Special演出を normalizedTime 監視のコルーチン化**(`ArcanaSpecialMotionCoroutine`): Charge=等倍・通常速度／Activate=拡大＋やや低速(0.65)／終盤(t≥0.85)はさらに低速(0.3)。**Specialを抜けた瞬間に等倍へ戻す**ことで「Activate後に一回り大きい別モーションが数フレーム見える」不具合を解消。
- 2026-06-08: **ヒーロー育成 Phase3＝ボスを主人公として採用＋ロスター選択（Cowork/Unity MCP）**。育成画面で主人公を選べるように。
  - **採用機構**: `IsActiveHeroUnit`/`IsHeroUnit`/`IsHeroCandidateUnlocked` を追加。盤面枠フリー・育成スケール・自動復活・盤面検出・ショップ重複除外・GrantHeroUnit/ChangeHeroUnit を「アクティブ主人公（ボス可）」対応に。基本3人は常時、章ボスは該当章クリア（HasBossAlly）で解放。ボスの必殺は default 経路。
  - **ロスターUI**: 育成画面をグリッド化（基本3＋章ボス13）。カードで主人公選択、解放済は強化、未解放は**シルエット＋「第N章クリアで解放」**。使用中は金枠＋「◆使用中」。Compilation OK（errCS=0）。
- 2026-06-08: **追加ヒーロー6体（各陣営の alt/3rd 将）＋陣営シナジー割当（Cowork/Unity MCP）**。
  - **6体**: HeroZiran/HeroBrome(Lyonar), HeroReva/HeroShidai(Songhai), HeroKara/HeroIlena(Vanar)。`UnitAnimationBuilder` で f1/f2/f6 の alt/3rd 将アトラスからビルド（cost2・Hero枠）。立ち絵=general_f*alt/third を `Resources/UI/HeroArt` へ。DB=71体。
  - **シナジー**: 各ヒーローに陣営シナジー(Lyonar/Songhai/Vanar)＋テーマ2種を割当（`ResolveDefaultSynergies`）。これで陣営シナジーが実ユニットに乗る。`LocalizationManager` にJA名追加。
  - **ロスター/解放**: `HeroRosterIds`＋`HeroUnlockChapters` に6体追加（進行度で解放＝ch2/3/4/5/6/7。暫定・調整可）。未解放は育成画面でシルエット＋解放条件表示。
  - **ショップ除外**: `IsLegionOnlySummonData` に "Hero"接頭辞ユニットの除外を追加（基本3＋追加6はショップ/敵/召喚に出ない。ボス将は通常ユニットのまま）。Compilation OK（errCS=0）。
- 2026-06-08: **出撃準備画面＋6陣営シナジーの土台（Cowork/Unity MCP）**。
  - **準備画面**: チャプター選択 → カード押下で `ShowChapterPrep(章)`（即開始せず準備画面へ）。左に主人公立ち絵、右に主人公情報＋「ショップに出すユニットを選ぶ（準備中＝将来のショップ編成入口）」＋「クエスト出発！」→`StartChapter`。戻る→チャプター選択。`LobbyUI.chapterPrepView`/`BuildChapterPrepView`/`CreateLobbyActionButtonOn`。
  - **陣営シナジー（土台）**: `SynergyType` に Lyonar/Songhai/Magmar/Vetruvian/Abyssian/Vanar を追加。`LocalizationManager.SynergyName`（JA/EN）、`SynergyManager` の `GetTierSummary`（効果説明）＋ `ApplyBattleStartSynergiesForTeam`（標準2/4/6で既存ヘルパによる効果）を実装。**まだユニットには未割当**（バランス影響なし。将来ユニット/シナジーを増やす際に割り当てる土台）。
  - 検証注意: errCS走査前に LogEntries.Clear() しないと途中オートコンパイルの旧エラーを拾う。clear→recompile→走査でerrCS=0確認。
- 2026-06-08: **メインロビー新設＋導線見直し（Cowork/Unity MCP）**。図鑑・ヒーロー育成はセーブデータ別なので、タイトルから外し、スロット選択後の「メインロビー」へ集約。
  - 遷移: タイトル(はじめから/つづきから/オプション/終了) → スロット選択 →（新規はヒーロー選択）→ **メインロビー**（プレイ/ヒーロー育成/図鑑/タイトルへ）→ プレイ＝モード選択(チャプター/コア戦) → チャプター選択。
  - `LobbyUI`: `mainLobbyView`＋`BuildMainLobbyView`/`ShowMainLobby`/`CreateLobbyMenuItem` 追加。SelectSlot/ConfirmHeroSelection を `ShowMainLobby` へ、モード選択・図鑑・ヒーロー育成の戻りを `ShowMainLobby` へ。タイトルから図鑑/ヒーロー育成項目を削除。Compilation OK（errCS=0）。
- 2026-06-08: **ヒーロー育成 Phase2＝手動強化システム（Cowork/Unity MCP, DESIGN_R3-hero-scale）**。
  - **セーブ通貨**: per-slot `heroPoints`＋per-hero `heroMastery.manualLevel`（SaveData/SaveManager, JsonUtility+List）。章クリアで `10+章×5`、敗北で `4+章×2` 付与。手動Lvは +3%/Lv（上限15）、コスト `20+Lv×10`。`HeroMetaBaseMultiplier` に合算。
  - **UI**: タイトルメニューに「ヒーロー育成」を追加（Options/Exitをずらして配置）。3ヒーローの手動Lv/ボーナス%/所持ポイントを表示し、ポイント消費で強化（`BuildHeroUpgradeView`/`RefreshHeroUpgrade`）。Compilation OK（errCS=0）。
- 2026-06-08: **ヒーローのスケール育成 Phase1（Cowork/Unity MCP, DESIGN_R3-hero-scale）**。ヒーローがランで育ち、盤面枠も取らない。
  - **盤面枠フリー**: `PlacedTeam1Count`/配置上限判定からヒーローを除外。常に追加で出せる（cost4/5★2化で上がった強要感を緩和）。
  - **ラン内Lv**: ウェーブクリアで「敵数+2」XP → Lv=1+XP/8（上限20）。`BaseEntity.heroScaleMultiplier`（ApplyCurrentStats内で非複利乗算）で HP/攻撃をスケール。倍率＝章クリア数×4%（メタ自動上昇）×Lv(+5%/Lv)×盤面ボーナス(出した味方×2%)。
  - **必殺スケール**: `HeroUltPower=1+(Lv-1)×6%` を `UseHeroUltimate`＋説明文の両方に適用（表示と実値一致）。
  - **自動★アップ**: Lv5→★2 / Lv10→★3（ショップ非経由、`ApplyHeroScaling`→`ApplyStarLevel`）。
  - **Phase2(未)**: 編成画面の手動育成ポイント（セーブ通貨＋UI）。DESIGN_R3-hero-scale に記載。Compilation OK（errCS=0）。
- 2026-06-08: **チャプター全体のバランス調整（★上限＋資源増量・Cowork/Unity MCP）**。ユーザー方針: cost4/5の★3はプレイヤー専用の強さとし、敵としては出さない。
  - **敵の★上限**: cost4・cost5の敵は全て★2上限に統一（章ボス含む）。汎用章(ch4-13)は `BuildScaledFactionChapter` に `const int e4=2` を導入し、エリート(E0-E2=cost5)/recruit4(R4=cost4)/escorts(cost5)/章ボスの配置・中ボス(3-5/3-10/4-5/4-7/4-8/4-9)スターを e4 に。章1-3は cost4/5 敵の★3を全て★2へ（Caliber/Solfist/Dissonanceの章ボス、Wraith/Wujin、4-7〜4-9中ボス）。cost3の★3（Wolfpunch/Kane/Paragon中ボス等）は限定箇所のみ許容で据え置き。難度はユニット構成と cost2/3 のスターで表現。
  - **資源増量（しっかり）**: `eventBonusGold` 8→14、`BonusItem` 2→3個、中ボスコイン報酬 `40+(ch-1)*20` → `70+(ch-1)*25`。汎用章の各ステージにコイン/アイテムのドロップ敵を増設（Stage1のコインも 3→5/4 に増額）。
- 2026-06-08: **ボス図鑑グリッドをユニットスプライト表示に統一（Cowork/Unity MCP）**。従来は `data.icon`（長方形の絵札）を表示しており、キャラ切抜きのボスと長方形アイコンのボスで見た目が混在していた。各セルで盤外にボス実体を生成し `CollectionBossAnimator` で待機アニメをセルへミラー（詳細画面と同方式）。未解放は暗色乗算のシルエット、解放後はフルカラー。`collectionPreviewEntities` で実体を管理し、グリッド再構築・図鑑離脱時に破棄。Compilation OK（errCS=0）。
- 2026-06-08: **章1ボスを Caliber-O に設定（Cowork/Unity MCP）**。章選択画面の表紙キャラを章1の章ボスに採用。
  - **新ユニット Caliber**: `UnitAnimationBuilder` に spec 追加（`f1_caliber-o`／T4・cost4・range1・近接の Lyonar 聖騎士将。idle14/move10/attack29/dead16）。`SynergyManager` case "caliber"＝Divine/Royal/Warrior。`LocalizationManager` JA名「キャリバー・O」。アイコンは Icon/T2→T4 へ複製（cost4 ルックアップに整合）。DB=65体・prefab/icon/frame 正常。
  - **章1ボスへ配線**: `ChapterBossRewardUnitIds[1]=Caliber`、ch1 の 4-10 ボス敵を Caliber★3 に（直前の Grymbeast から差替＝「倒したボス＝もらえる」を維持）。
  - **戦前ダイアログ**: `HeroBossDialogueUI` に専用立ち絵ロード（`Resources/UI/BossArt/<bossId>`、general_boss_1 を Caliber として取り込み）＋ Caliber の挑発/締めセリフ（JA/EN）。Compilation OK（errCS=0）。
- 2026-06-08: **将系ユニットをプレイアブルから除外（温存・Cowork/Unity MCP）**。ヒーロー/ボス将のアートを流用した「将系」が入手可能になっていたため、後の専用枠として温存。
  - **対象8体**: `ReservedGeneralUnitIds`＝Skindogehai/Tier2general/Altgeneraltier2/Embergeneral/Plaguegeneral/Snowchasermk/Maehvmk/Ilenamk2。`IsReservedHeroFormUnit` をこの集合に拡張し、`IsLegionOnlySummonData` にも配線（ショップ/開始抽選/召喚/ランダムウェーブ候補から除外）。
  - **獲得経路の除去**: 勧誘候補（RecruitMidBoss 2-5/2-10×3）・勧誘プール（AbyssianRecruit3, FinaleRecruit4）・開始cost3（Cost3StarterPlayable）・旧 bossRewardUnitIds・章ボス報酬（ch1=Grymbeast／ch3=Dissonance に差替）を非general化。ch1/ch3 の 4-10 ボス敵も報酬と一致（Grymbeast★3／Dissonance★3）。`ShowMidBossRecruit` に将系/ヒーロー除外の保険フィルタを追加。
  - **敵としての登場は維持**: StagedCombat の将系 WaveEnemyPlacement（伏線）はそのまま。プレイアブル（入手）からのみ除外。Compilation OK（errCS=0）。
- 2026-06-05: **ヒーロー専用3体 実装・第1弾（基盤コード／DESIGN_R3-hero-units・Claude Code）**。アセット生成は Unity 接続待ち。
  - **確定仕様**: Aldin（f1・聖騎士守護・Divine/Guardian/Royal）/ Kagachi（f2・**アサシン**・Shadow/Frenzy/Wraith）/ Vesna（f6・**蒼炎**・Inferno/Frost/Arcanist）。内部ID `HeroAldin/HeroKagachi/HeroVesna` 固定。ユーザー指定でカガチ=アサシン系、ヴェスナ=青い炎（雷→炎）に変更。
  - **ビルダー specs 追加**: `UnitAnimationBuilder.PlistAtlasSpecs` に3体（tierFolder=Hero, cost2, f1/f2/f6_general）。reference から自動コピー。**ビルド実行は Unity 接続後**。
  - **排他制御（設計「最重要」）**: `IsHeroUnitId`/`IsHeroUnitData` 新設。`IsLegionOnlySummonData` に集約してショップ/敵ウェーブ/ランダム召喚/エリート召喚/開始抽選を一括除外。売却は `TrySellEntity` で個別拒否。ボス報酬/仲間化は ID表に不追加で自動除外。
  - **シナジー**: `SynergyManager.ResolveDefaultSynergies` に3体。**ローカライズ**: `LocalizationManager` JA（アルディン/カガチ/ヴェスナ）＋EN 表示整形。
  - **必殺3種**（`UseHeroUltimate` を heroUnitId で分岐＋`GetHeroUltimateName`、`HeroUltButtonUI` のラベルをヒーロー別必殺名に）: Aldin聖盾（味方全体に最大HP30%シールド＋被ダメ-20%）/ Kagachi修羅（味方全体に与ダメ+35%＋攻速×1.25）/ Vesna蒼炎（敵全体に青炎80+章×40＋味方攻速×1.15）。Vesnaの青炎VFXは暫定 Frost 代理（TODO: 専用青炎VFX）。
  - **専用通常スキル3種**（`BaseEntity` dedicated＋`UnitStatusPanelUI` 実数値説明）: Aldin「聖壁」(自他シールド＋小回復＋被ダメ軽減)/ Kagachi「残影斬」(最遠の敵へ踏込2連斬・★3で3連＋自己加速＝アサシン)/ Vesna「蒼炎槍」(対象中心AoE青炎＋燃焼・遠隔)。数値は全て既存スキルヘルパ経由（説明と実値一致）。
  - **hero-select 基盤**: `SaveData.heroUnitId`（空=未選択・後方互換）＋`SaveManager.GetHeroUnitId/SetHeroUnitId`。`GameManager.GrantHeroUnit` を選択ヒーロー採用に（空/不明→既定HeroAldin→未ビルドはランダム付与へ安全フォールバック）。
  - **hero-select UI（A）**: `LobbyUI.heroSelectView` 新設（`SelectSlot` で主人公未選択なら割込→3択カード＝名前/ロール/必殺説明/「性能」プレビュー/確定→`SetHeroUnitId`→ロビー、未ビルド時もnull安全）。
  - **ヒーロー変更（B）**: `GameManager.ChangeHeroUnit`（既存ヒーロー除去→保存→新付与・戦闘中不可・装備はベンチへ）＋ `CurrentHeroUnitId`。新規 `HeroChangeUI`（独立ルートCanvas・3択・現在強調）を OptionsPanel「ヒーロー変更」ボタンから起動（編成フェーズのみ実変更）。
  - **形態変化温存（C）**: `IsReservedHeroFormUnit`（Skindogehai=カガチ系/Tier2general=ヴェスナ系）を `IsEntityUnlockedForShop`/`CanBuyEntity` で**ショップ購入/抽選から除外**（敵ウェーブ・既存勧誘は維持＝バランス/参照を壊さない）。
  - **未実装（Unity接続後）**: ① **アセットビルド**（アニメ/プレハブ/DB生成）。② **C のフォルダ移動**（Skindogehai/Tier2general を T3→Hero フォルダへ）は `AssetDatabase.MoveAsset`＋builder のテンプレートパス更新（Solfist の template が Skindogehai.prefab 参照）が必要なため Unity 下で安全実施。③ heroSelectView/HeroChangeUI の待機アニメプレビュー化（アセット後）。
- 2026-06-05: **ヒーロー3体 アセットビルド＋温存ユニット移動 完了（Unity MCP 経由・Claude Code）**。Unity 再接続後に実行。
  - **コンパイル不具合の修正**: 当方の `LobbyUI.BuildHeroCard` が `FirstOrDefault`/`StringComparison` を使用したが、Cowork版 LobbyUI は `using System;/System.Linq;` 非導入（フル修飾方針）のため Assembly-CSharp 全体がコンパイル失敗していた → 手動ループ＋フル修飾に修正。以後コンパイル成功・エラー0。
  - **アセットビルド**: `UnitAnimationBuilder` をメニュー実行。HeroAldin/HeroKagachi/HeroVesna のスプライト（Y反転スライス）/prefab/AnimatorController/DB を生成。DB=55ユニット、ヒーロー3体 cost2・シナジー（Divine/Guardian/Royal・Shadow/Frenzy/Wraith・Inferno/Frost/Arcanist）正常反映、壊れた参照0。
  - **HeroVesna テクスチャ修正**: `reference/.../resources/units/f6_general.png` が 1024×512（plist 想定 512×1024 と転置）でフレーム大量欠落 → `original_resources/units/f6_general.png`（正512×1024）で上書き再ビルド → idle 14 等フル取得。
  - **温存ユニット移動（C）**: `AssetDatabase.MoveAsset` で Skindogehai/Tier2general の prefab＋スプライトフォルダを T3→`Hero/` へ移動（GUID保持＝DB参照維持）。builder spec の tierFolder を Hero に、Solfist の template path と AutoRunner の必須prefabパスも Hero に更新（再ビルド時の重複再生成防止）。
  - 残: heroSelect/HeroChange の待機アニメプレビュー化（任意・現状はアイコン静止）、実機プレイ確認、数値の R3-balance。
- 2026-06-05: **【最重要・真因】報酬選択が点滅・進行不能になるバグの根本修正（Claude Code, プレイヤー報告対応）**。
  - **症状**: 獲得オーグメントの詳細パネル（`AugmentTooltipUI`）を表示中にボス報酬選択が出ると、報酬の表示状態がオーグメントのホバー（＝ツールチップの active 状態）に1:1連動して点滅し、選択不能で進行ハマり。
  - **真因**: `BossRewardSelectionUI`/`ItemRewardSelectionUI`/`ChapterRosterUI`/`BuffTileRewardUI` が `EnsureExists` で `FindObjectOfType<Canvas>()` の**子**として遅延生成されていた。報酬発生時（＝プレイヤーがオーグメントをホバー中）には `AugmentTooltipUI` の Canvas が生成・active なため、`FindObjectOfType<Canvas>()` がそのトグルするツールチップ Canvas を返し、報酬UIがその子になる。→ `AugmentTooltipUI.Hide()`（ホバー外し）で親ごと `SetActive(false)` され、報酬がツールチップのホバーに連動して点滅・消滅。点滅報告の無い `AugmentSelectionUI` は元から独立ルート Canvas のため無傷だった。
  - **修正**: 4つの報酬/選択UIを**独立ルートの ScreenSpaceOverlay Canvas**（Canvas＋CanvasScaler[ConstantPixelSize]＋GraphicRaycaster を自前生成、外部 Canvas へ親子化しない）に変更。`LobbyUI`/`RoundProgressUI` も同APIだがツールチップ生成前に1度だけ生成されるため非該当・据え置き。
  - **副次の堅牢化（強化マス3-5）**: `BuffTileRewardUI` バナー段階で overlay の `raycastTarget=false` にして盤面クリックを通そうとしていたが、盤面確定は `GameManager.HandleBuffTilePlacementClick`（Update から raw `Input`＋`ScreenToWorldPoint`＝UI非依存）で行うため overlay を切る必要が無かった。切ると右上HUDが露出するため**raycast を維持**（HUD/ショップを遮断・盤面確定は world 座標）、視認性のため減光のみ 0.6→0.18。
  - **ヒーロー必殺ボタンが FIGHT に重なる**: FIGHT(scene: bottom-left基準 1580,215 / 300×100 ＝ x[1430-1730]・y[165-265]) と `HeroUltButtonUI`(-40,168 / 220×66 ＝ x[1660-1880]・y[168-234]) が重複。右側は190pxしか無く220px幅が入らないため、**FIGHT の真上**(-230,292 ＝ 中心x1580・下端292)へ移動。
  - **アイテム3択カードの崩れ**: カード内でアイコン(0.62中心96px ＝ y138-234)が名前ラベル(y120-156)に重なっていた。アイコンを上(0.717・84px)へ、名前(0.47-0.575・自動縮小)、効果(0.05-0.45・折返し)に再配置して非重複化。パネルも 780×420→820×440（前回拡張）。
- 2026-06-05: **新実装UIのレイアウト是正・第1弾（§9 Cowork実装ぶんの仕上げ／Claude Code）**。実機未検証の新UIを静的解析（rectサイズ vs 内容量・アンカー・折返し）でレビューし、確実なはみ出しを修正。
  - **図鑑 育成パネルの節目テキスト（§9.2 #15 名指し）**: `LobbyUI` の `cdAffCount` がパネル右下48%幅・1行・折返し無効に長文「節目 ●Lv3… ●Lv5… ●Lv8…」を詰めて右寄せ→左へあふれ `cdAffLv` に重なっていた。育成パネルを3段（見出し＋ボーナス／育成Lv／節目）に再配置し、節目を**全幅・左寄せ・折返し有効**に。区切りを「 / 」化。
  - **中ボス アイテム3択UI**: `ItemRewardSelectionUI` のパネル 780×420 に対し3カード(220×3＋spacing26×2＝712)＋余白80＝792 で約12px はみ出し・高さも4px不足 → パネルを **820×440** に拡張（内側 740×316 で余裕）。
  - 静的に問題無しを確認（修正不要）: `WaveClearBannerUI`（中央カード）/`CoreModeHudUI`（左右コアバー420＋中央フェーズ620は1920で非干渉）/`BuffTileRewardUI`（カード648≤内側688）/`FormationHintUI`（body254≥行252）。
  - **実機判断が必要（Cowork領域として残置）**: `HeroUltButtonUI` の位置(右下 -40,168)が FIGHT/ショップと干渉しないか、`FormationHintUI` トグル(左上 232,-8)が `RoundProgressUI` と被らないか。これらは別CanvasScalerモード(ScaleWithScreenSize)で他HUD(ConstantPixelSize)と座標系が異なり、静的比較不能。
- 2026-05-31: **ロビー仕上げ（DESIGN_lobby.md 引き継ぎ#1#2 実装）**（Claude Code, GameManager 章まわり＋OptionsPanelUI のみ・Cowork非干渉領域）。
  - **#1 ラン初期化の遅延**: `GameManager.Start` に `willShowBootLobby` 判定を追加し、起動ロビーを被せる時は `GrantStartingUnit`/`TryShowChapterRoster`/`TryStartEventRound` を実行しない（章選択でシーン再読込→選んだ章で改めて初期化されるため、二重初期化の無駄を排除）。章を明示選択して来た場合（`startedFromLobbySelection`）は従来どおり即初期化。
  - **#2 「ロビーへ戻る」導線**: `GameManager.RequestReturnToLobby()`（`PendingStartChapter=0`＋静的フラグ `ForceLobbyOnNextBoot=true` を立ててシーン再読込＝次回起動でロビー表示）を新設。OptionsPanel にバイオレット色の「ロビーへ戻る / Return to Lobby」ボタンを追加（Help/Restart/Close の y を再配置、JA/EN 対応）。`showLobbyOnBoot` が false でも `ForceLobbyOnNextBoot` で確実にロビーへ戻る。
  - 既定値 `showLobbyOnBoot=false` は据え置き（挙動不変・回帰リスク無し）。ON化＆実機検証は Cowork 依頼（当方は Editor 再コンパイルを起動せず）。
- 2026-05-31: **チャプター2 静的検証（DESIGN_handoff_claudecode.md §A）**（Claude Code, 読み取り専用・コード変更なし）。`BuildChapter2Rounds` の全配置をスクリプト照合：①名前指定ユニット20種が `Entity Database.asset` に各1件存在、②全86配置の座標が列6-10・行3-7内（範囲外ゼロ）、③ステージ構成 3/10/10/10=33ラウンド、④章ボス 4-10=Skyfalltyrant＋`ChapterBossUnitIds[2]` 整合（章クリア→`AddBossAlly` は章1と同一経路）。**バグ無し＝GameManager 修正不要**。残りの実進行・難易度体感・ロビーアートの見た目（§C の明るさ/歪み/スクショ/フォント）は実機判断のため Cowork 領域。`LobbyUI` 回転 `Update` は GameObject が Hide 時 SetActive(false) のため非表示中はそもそも tick せず、§C-3 のガードは不要（冗長コード追加を回避）。
  - **放射光の延長**: 終盤に`AttackEffectPlayer.PlayArcanaActivateLightRays`で放射状バースト(Free3/652)を本体中心から拡大＋回転表示し、スプライト端で途切れる光を外側へ補完。
  - **controller**: Special→Idle遷移を exitTime0.95→0.99 / duration0.05→0 にし、最後のコマまで再生してから等倍Idleへ即切替。Compilation OK（Errors 0）。
- 2026-05-31: **チャプター2 ＆ ロビー画面（一次実装）**（Claude/Cowork+Unity MCP、検証はClaude Codeへ引き継ぎ）。
  - **チャプター2**: `GameManager.BuildChapter2Rounds`（全33R・4ステージ・章1座標規約準拠）を追加、`BuildChapterRounds` に case2、`ChapterBossUnitIds` に `{2,"Skyfalltyrant"}`。既存登録ユニットのみ使用（Arcana未登録のため不使用）。最終4-10=Skyfalltyrant★2＋大護衛（章1ボスLegion等）。
  - **ロビー/タイトル**: `LobbyUI.cs` 新規（ChapterRosterと同じプログラム生成・EnsureExists・sorting60050）。タイトル＋章選択（`SaveManager.IsChapterUnlocked`で解放判定・ベスト併記）＋ボス仲間アイコン一覧＋オプション/終了。`GameManager` に `PendingStartChapter`(static)・`showLobbyOnBoot`(既定false)・`RequestStartChapter(int)`（timeScale=1＋シーン再読込で章開始）・`Start`の受け渡し/ブート表示フックを追加。**既定OFFで従来挙動は不変**。
  - 設計書 [DESIGN_chapter2.md] / [DESIGN_lobby.md]、Claude Code向け検証・仕上げ手順を [DESIGN_handoff_claudecode.md] に追記。Compilation OK（Errors 0）。プレイ検証は未実施。
- 2026-05-31: **設定のGameScene適用（HUD表示／キーコンフィグ）＋カード素材リネーム**（Claude/Cowork+Unity MCP）。
  - **HUD表示適用**: `HudSettingsApplier.cs` 新規（EnsureExists・`SettingsStore.OnChanged` 購読で即時反映）。`SettingsStore.GetHud(synergy/round)` で SynergyPanelUI/RoundProgressUI のルートを SetActive 切替、`coin` OFF で CoinIncomePanelUI を閉じる、`tooltip` OFF で開いているツールチップを閉じる。各 `Show`（SynergyTooltipUI/ItemTooltipUI/AugmentTooltipUI/CoinIncomePanelUI）冒頭に `GetHud` ゲートを追加し新規表示を抑止。`GameManager.Start` で `SettingsStore.ApplyAll()` ＋（起動ロビー非表示時のみ）`HudSettingsApplier.EnsureExists()`。
  - **キーコンフィグ適用**: OptionsPanel 開閉を `SettingsStore.GetBind("toggleOptions", KeyCode.Escape)`、デバッグウェーブを `GetBind("debug", debugTrainingWaveHotkey)` で読むよう置換（既定値は従来どおり Escape / F8）。
  - **カード素材リネーム**: `Assets/Resources/UI/Cards` の6枚を `silver/gold/prism` の `back/front` へ改名し、`AugmentSelectionUI.TierBackSprite/TierFrontSprite` の文字列を新名へ更新。未使用 `Assets/Resources/UI/Lobby`（LobbyUI は Play/ 素材を使用し非参照）を削除。
  - Compilation OK（ゲームスクリプトの CS エラー0／警告は既存のみ）。実機での表示切替・キー再割当の体感検証は Cowork 依頼。
- 2026-05-31: **UIアート差し替え3点（背景／リザルト枠／オーグメントkindアイコン）**（Claude/Cowork+Unity MCP）。
  - **ロビー背景の複数化＋ランダム化**: duelyst `scenes/` の風景5枚を `Assets/Resources/Play/LobbyBg/`（lobby_storm/skycity/woods/highlands/nightfall）へコピーしSprite化。`LobbyUI.EnsureBackground` を `Resources.LoadAll<Sprite>("Play/LobbyBg")` から起動ごとにランダム選択に変更（読込ゼロ時は従来 `Play/play_background` にフォールバック）。背景の暗さ補正を 0.66→0.80 に緩和。
  - **リザルトパネルに素材**: `frame_quest.png` を `UI/Panels/result_panel.png` として配置（9-slice border=30/46/30/40、textureType=Sprite）。`ResultPanelUI.BuildUi` でベタ塗り矩形→9-sliceパネル＋内側に半透明暗幕(InnerFill)でテキスト視認性を確保。素材欠落時は従来ベタ塗りにフォールバック。
  - **オーグメントkindアイコンの単一化**: `kind_stat/synergy/combat/special` は256×256のスプライトシートをSingleのまま丸ごと表示していた不具合。各シートから代表アイコン1個を抽出して同名PNGへ差し替え（コード/参照変更なし、spriteMode=Single）。`kind_economy/item` は元から単一のため不変。
  - Compilation OK（ゲームスクリプトのCSエラー0）。実機での見た目（背景の明るさ/テキスト視認性/アイコン解像感）確認は Cowork 依頼。
- 2026-05-31: **R2-recruit 設計起票**（Claude/Cowork）。中ボス＝同コスト2-3体を出現させ撃破後1体を章内解放、章ボス＝選択なしで恒久解放、解放はショップ出現のみ（無料ベンチ無し）。既定ロック＝cost3の6体＋cost4/5全。章ボス報酬は ch1-5=cost4 / ch6+=cost5。[DESIGN_R2-recruit.md](DESIGN_R2-recruit.md) 起票。実装可能スコープは章1・2＋全体フレーム（章3-5以降とcost4増は R2-chapters）。R1-meta のベンチ無料配置(TryShowChapterRoster)はショップ恒久解放へ仕様変更予定。
- 2026-05-31: **コスト4ユニット +5体追加（完全新規）**（Claude/Cowork+Unity MCP）。cost4を5→10体に。`UnitAnimationBuilder.PlistAtlasSpecs` に Frostwarden(f6_general:Frost/Guardian/Royal)・Duskblade(f2_general:Shadow/Ranger/Frenzy・遠)・Emberlord(f5_general:Inferno/Beast/Frenzy)・Suncleric(f1_general:Divine/Royal/Guardian)・Sanddjinn(f3_general:Arcanist/Storm/Summoner・遠)を追加し、duelyst将官スプライトから sprite/prefab/anim を自動生成。`SynergyManager.ResolveDefaultSynergies` に5体のcase追加。**重要**: `SyncEntityDatabase` を全置換→**マージ方式**に修正し、spec外の Arcana/Zyx が消えないようにした。DB=全52体・cost4=10体・コンパイル0で検証済み。固有スキル/JA-EN名/専用アイコンは後続（[DESIGN_cost4-units.md](DESIGN_cost4-units.md)）。R2-recruitのcost4不足は解消（章ボス報酬5体＋中ボス候補5体に分離可）。
- 2026-05-31: **cost4追加ユニットを general系→固有スプライトへ差し替え**（Claude/Cowork+Unity MCP）。指摘どおり `f*_general` 系は同一ヒーローの形態/スキン違いで既存(Tier2general/Embergeneral/Plaguegeneral等)と見た目が被るため、初版5体(Frostwarden/Duskblade/Emberlord/Suncleric/Sanddjinn)を**生成物・DBごと削除**。見た目が明確に異なる Grymbeast(boss_grym:Shadow/Beast/Abyss)・Cinderwraith(boss_unhallowed:Inferno/Wraith/Frenzy)・Draugarlord(f6_draugarlord:Frost/Machine/Guardian・遠)・Kingsguard(f1_kingsguard:Divine/Royal/Guardian)・Dissonance(boss_dissonance:Arcanist/Storm/Summoner・遠)に差し替え。DB=全52体・cost4=10・Arcana/Zyx保持で検証済み。[DESIGN_cost4-units.md] 改訂。
- 2026-05-31: **cost4新5体に固有スキル＋JA/EN名を付与**（Claude/Cowork）。`add-unit-skill.md` 準拠で5固有スキルを実装（数値は既存ヘルパ経由で説明文と一致）: Grymbeast=影喰らいの牙(跳躍AOE+吸命)／Cinderwraith=業火の狂乱(自中心AOE+延焼+自己加速)／Draugarlord=氷塊斉射(遠AOE+減速凍結+自己シールド)／Kingsguard=王命の盾(回復+味方シールド+被ダメ軽減)／Dissonance=不協和音の連弾(★で3/4/5発の雷弾+連鎖)。`BaseEntity.TryExecuteDedicatedSkill`へ5case＋Executeメソッド、`UnitStatusPanelUI`へ実数値スキル文(JA/EN)、`LocalizationManager`へJA名5件。汎用フォールバックには未登録（新規IDで除外不要）。アイコンはユーザー用意済み。Compilation OK（CSエラー0）。残: stat/サイズの実機調整(R3-balance)。[DESIGN_cost4-units.md] 完了。
- 2026-05-31: **R2-recruit 実装（中ボス/章ボスの仲間化・ショップ解放）**（Claude/Cowork+Unity MCP）。`GameManager`中心。①解放2層化：`chapterUnlockedUnitIds`(章内)＋`HasBossAlly`(恒久)。②`IsEntityUnlockedForShop`書換でcost1-2常時/cost3はスターター4体のみ常時/他cost3・cost4・cost5は解放時のみショップ出現。③`WaveDefinition.RecruitCandidateIds`＋`RecruitMidBoss`ヘルパで章1-2の全14中ボスを同コスト候補3体出現（stage2=cost3/stage3-4=cost4新5体）に差し替え、クリア時に1体を章内解放（無料ベンチ無し）。④章ボス4-10をcost4報酬(ch1=Snowchasermk★3/ch2=Solfist★3、旧cost5は護衛)に変更し3択廃止、章クリアで`AddBossAlly(報酬)`＝恒久解放。⑤R1-metaの自動ベンチ配置(TryShowChapterRoster)撤去（解放＝ショップ恒久出現へ）。Compilation OK（CSエラー0）。実機検証・balanceはR3、章3-5/cost5章ボスはR2-chapters。[DESIGN_R2-recruit.md] 実装記録追記。
- 2026-05-31: **R2-recruit 仕上げ（カード選択UI＋ベンチ配置＋cost5ショップ除外の確定）**（Claude/Cowork+Unity MCP）。①ボス（中ボス）選択UIをカード見た目に：reference `card_backgrounds/craftable_unit.png` を `UI/Cards/unit_card`(Sprite)として取込み、`BossRewardSelectionUI.CreateOption` をユニットカード枠＋肖像窓アイコン＋名前「仲間にする/RECRUIT」へ。②`OnMidBossRecruit` で選択時に `CreateBenchEntity` で**1体ベンチ配置**（章内ショップ解放は維持）＝設計の「無料配置なし」を「1体配置」へ変更。③cost5がショップに出る件は `IsEntityUnlockedForShop` 上は既にロック済みで、原因は**旧R1-meta時代の章クリアで `bossAllies` に残った cost5(Legion/Skyfalltyrant)** だったため、セーブから cost5 boss仲間のみ除去（save.json.bak バックアップ作成・章記録/ cost4 Snowchasermk は保持）。Compilation OK（CSエラー0）。
- 2026-05-31: **R1-saveslots 実装（セーブスロット3枠＋削除）**（Claude/Cowork）。保存層をスロット対応に：`ISaveStore.Delete`、`LocalJsonSaveStore(baseDir,fileName)`＋`Delete`、`SaveManager` に `SlotCount=3`/`ActiveSlot`/`SetActiveSlot`/`DeleteSlot`/`GetSlotInfo` と旧 save.json→save_0.json 移行、アクティブスロットを PlayerPrefs 永続。LobbyUI：タイトル PLAY→スロット選択ビュー（3カード＝到達章/ベスト/仲間/保存日時、選択でSetActiveSlot→ロビー、削除は確認オーバーレイ付き）。JA/EN対応。Compilation OK（CSエラー0）。[DESIGN_R1-saveslots.md] 起票。実機検証は残。
- 2026-05-31: **スロットカードのデザイン改善＋作業フォルダ掃除**（Claude/Cowork）。スロット選択カードを単色→**パネル素材(frame_quest=UI/Panels/result_panel の9-slice)＋上部にシーン画像(Play/LobbyBg をスロット別)＋暗幕＋濃色テキスト**へ刷新（新規取込なし・既存素材流用）。Compilation OK。作業用一時フォルダ `_tmp_art`（アイコン抽出のプレビュー類）を削除。空の `Assets/Resources/UI/Lobby` 殻はマウント都合で残置だが .meta 無し・Unity非対象で無害。
- 2026-05-31: **Scripts 分類方針を確定（適用は今後の追加時／一括移行は要相談）**（Claude/ユーザー合意）。現状 `Assets/Scripts` 直下に47本がフラット。今は一括移動しない（Codex同時編集との差分衝突を避けるため）。今後スクリプトが増える時は次のサブフォルダへ配置し、折を見て既存も移す：`Core/`(GameManager,BaseEntity,Melee/RangedEntity,GridManager,Graph,Tile,Manager,PlayerData,Draggable,Healthbar,EntitiesDatabaseSO) / `Synergy/` / `Augments/` / `Items/` / `UI/`(各Panel/HUD/Tooltip/Shop/Lobby等) / `Effects/`(AttackEffectPlayer,BloodMageVisual) / `Localization/` / `Save/`(既存)。Unityは「フォルダ=名前空間」ではないため移動は .meta ごとならコード変更不要・参照(GUID)維持。一括移行を実施する場合は Codex 作業と衝突しないタイミングで。
- 2026-05-31: **R1-collection 実装（ボス図鑑＋ボス育成アフィニティ）**（Claude/Cowork）。①育成：`BossAllyRecord.recruitCount` 追加、`AddBossAlly` で章再クリアごとに加算。`SaveManager.GetBossAffinityLevel/StatMultiplier`（Lv1=等倍、以降+6%/Lv・上限+60%／暫定）。`BaseEntity.ApplyBossAffinityMultiplier` を `GameManager.CreateBenchEntity` の所持ボス生成時に適用（プレイヤー経路のみ・敵不変）。②図鑑：タイトルに「図鑑」メニュー追加、`GameManager.GetAllChapterBossRewardUnitIds()`(static) を元にロビーでグリッド表示（解放=アイコン+名前+育成Lv/ボーナス、未解放=シルエット+???）。Entity DBは Resources からロード。旧セーブは Lv1 後方互換。Compilation OK（CSエラー0）。[DESIGN_R1-collection.md] 起票。倍率はR3-balance調整。
- 2026-05-31: **ロビーBGM＋cost4新5体のアイコン割当**（Claude/Cowork+Unity MCP）。①BGM: `AttackEffectPlayer` に `PlayBgm(params paths)` / `PlayBattleBgm()` を追加。`LobbyUI.ShowTitle` でメニュー曲（music/mainmenu_v2c_looping→music_mainmenu_lyonar→music_collection の順で最初に見つかった曲）をループ再生、`GameManager.Start`（ゲーム開始・ロビー非表示時）で `PlayBattleBgm()` によりバトル曲へ復帰。②アイコン: ユーザー用意の `Assets/Images/Units/Icon/T4/{Grymbeast,Cinderwraith,Draugarlord,Kingsguard,Dissonance}.png`（取込済Sprite）を Entity Database の各icon欄へ割当（atlasフォールバックから専用アイコンへ）。Compilation OK（CSエラー0）。
- 2026-05-31: **BGM不発の修正（m4a重複の除去）**（Claude/Cowork）。`Assets/Resources/music` に ogg/wav と**同名の m4a が22個**あり、Unityはm4a非対応のため `Resources.Load<AudioClip>` が空クリップ(m4a側)を拾ってBGMが鳴っていなかった。m4a22個(+.meta)を削除（全て ogg/wav と重複・音源消失なし）。再取込で mainmenu_v2c_looping(97s)/music_mainmenu_lyonar/music_collection/music_battlemap01/music_playmode 等が AudioClip として正常ロードを確認。CSエラー0・playmode可。
- 2026-05-31: **ロビーBGM不発の根因修正（起動順）**（Claude/Cowork）。プレイモードで調査→ `AttackEffectPlayer` の自動BGM(RuntimeInitializeOnLoadMethod/AfterSceneLoad)が LobbyUI.Awake のロビーBGM設定を**後から戦闘曲で上書き**し、かつ初期化タイミングで再生に至っていなかった。LobbyUI に `Start()` を追加し、表示中なら `PlayLobbyBgm()` を bootstrap 後に再実行（ShowTitle 用に PlayLobbyBgm ヘルパ化）。実機(LobbyScene)で `mainmenu_v2c_looping` が isPlaying=True/vol0.26 で鳴ることを確認。※Game ビューの「Mute Audio」トグルが ON だと聞こえない点に注意。
- 2026-05-31: **盤面まわり3機能（友人案①〜③）実装**（Claude/Cowork）。⑥図鑑詳細: 解放済みセルをクリックで `UnitStatusPanelUI.ShowPreview`（ポートレート＋実数値スキル＋ステータス）。②配置フォーメーション: 戦闘開始時に盤上プレイヤー配置を検出、横一列3体=「突撃」(攻速+18%/与ダメ+15%)・縦一列3体=「鉄壁」(被ダメ-12%/シールド)を時限付与（`GameManager.ApplyFormationBonuses`、既存バフ機構流用）。③b転がる巨大物: 新規 `RollingHazard.cs`＝戦闘中に1行を転がり敵味方問わず最大HP30%ダメージ+1.2sスタン、中ボス戦中に2回出現（`LaunchRollingHazard`/`MidBossHazardRoutine`、手続き生成スプライトで素材不要）。Compilation OK（CSエラー0）。数値・演出・UIはR3。[DESIGN_board-gimmicks.md]/[DESIGN_R1-collection.md] 追補。
- 2026-05-31: **R2-coremode 設計起票（コア破壊モード）**（Claude/Cowork）。友人案④。敵味方にコア拠点を配置し敵コア破壊=勝利/自コア破壊=敗北の新モード。`GameMode`(Chapter/CoreAssault)で分岐し章モードを温存、コア=移動/攻撃しない被ターゲット可のBaseEntat拠点、勝敗はコアHP0で判定。ロビーにモードカード追加。[DESIGN_R2-coremode.md] 起票（RELEASE_PLAN R2-coremode 追加）。実装前に戦闘形式(単発/攻城)・ターゲット優先・報酬連携など5点の確認待ち。
- 2026-05-31: **R2-coremode 設計確定**（Claude/ユーザー決定）。コア破壊モードの仕様確定：開始時コイン＋cost1付与／1波目のみ手動FIGHT・以降は戦闘終了→インターバル5s→編成40s→次波自動／コアHPは波持ち越し／ターゲットは最も近い敵・オブジェクト(コア含む、例外可)／自コア破壊で即敗北・敵コア破壊で勝利／ボス解放は「区切りのいいステージ」(暫定5波ごと)で実施。[DESIGN_R2-coremode.md] を確定版に更新。次は実装着手。
- 2026-06-02: **R2-coremode Phase 1 実装（コア破壊モードの土台）**（Claude/Cowork+Unity MCP）。`GameMode{Chapter,CoreAssault}`＋`PendingMode`（PendingStartChapter と同方式）＋`CurrentMode`/`IsCoreMode` を `GameManager` に追加し、章モードは全て `!IsCoreMode` ゲートで温存。`BaseEntity.IsCore`/`ConfigureAsCore(int)`（無移動・シナジー無し・高HP6000・1.5倍スケール／Setup後に呼ぶ）、Melee/RangedEntity の Update 冒頭で `if(IsCore)return;`。`InitializeWaveDefinitions`→`BuildCoreModeRounds`(敵ウェーブ1)、`SpawnCores/SpawnCore`（自陣 列1,行5・敵陣 列10,行5、見た目 Borealjuggernaut 流用）を起動初期化で生成。`UnitDead` 冒頭でコア破壊検知→敵コア=`HandleCoreModeVictory`(ResultPanel流用)/自コア=`TriggerGameOver`。`HandleRoundTimeout` はコア戦で無効、`ClearEnemyUnits` はコアを残す、`PlacedTeam1Count` はコア除外。LobbyUI の「ボスラッシュ(Coming Soon)」カードを「コア戦/CORE WAR」へ転用し `StartCoreMode()`(PendingMode=CoreAssault→GameScene)。Compilation OK（CSエラー0）。**実機の1戦通し確認は未実施**：MCP eval ではシーン遷移(LoadScene)を駆動できず、ロビー「コア戦」カードから手動スモークが必要。次は Phase 2（戦闘終了→5s→40s→次波自動進行・敵波供給）。[DESIGN_R2-coremode.md] Phase1 記録追記。
- 2026-06-02: **R2-coremode Phase 2 実装（自動ウェーブ進行）**（Claude/Cowork+Unity MCP）。1波目のみ手動FIGHT（`DebugFight`がコア戦では`StartCoreWave`へ分岐）、以降は「戦闘終了→インターバル5s→編成40s→次波自動」を `CompleteCoreWave`＋`CoreAutoAdvanceRoutine` で実装（編成中FIGHTで早期開始可、コルーチンは `IsRoundInProgress`/`gameOver`/`coreModeResolved` で中断）。勝敗はコア破壊のみ＝`TryEndRound` をコア戦分岐（敵コア以外を一掃でウェーブクリア／味方全滅では敗北にしない）し、`HasLivingEnemyBattleUnit`/`HasLivingPlayerBattleUnit` をコア除外。コアHP持ち越し＝`SnapshotPlayerBoardUnits` でコアを除外（波間の全回復対象外）。無限ウェーブ＝`EnsureCoreWaveAvailable`/`BuildCoreWave(idx)`（数3+idx上限10・スター1+idx/4上限3・idx2+でcost2混在・idx6+でcost2主体・3体に1体遠距離・列7-9でコアと非重複）。波クリアごとに収入＋経験値＋ショップ無料リロール（章と同等）。編成カウントダウンは暫定で `ScorePopupUI` のフェーズ告知（専用HUDバーは Phase 3）。Compilation OK（CSエラー0）。**実機の通し確認は要手動**。次は Phase 3（区切り=5波ごとのボス解放 `AddBossAlly`／コアHPバーUI／編成カウントダウンHUD／balance）。[DESIGN_R2-coremode.md] Phase2 記録追記。
- 2026-06-02: **R2-coremode Phase 3 実装（区切りボス解放＋コア戦HUD）**（Claude/Cowork+Unity MCP）。①区切りボス解放：`CompleteCoreWave` で5波クリアごと `ReleaseNextCoreModeBoss()`＝`GetAllChapterBossRewardUnitIds()` 順に未解放ボスを1体 `SaveManager.AddBossAlly(id,1)` 恒久解放＋`ScorePopupUI` 告知＋`OnRosterChanged`（以降ショップに出現＝R1-collection連携）。②コア戦HUD：新規 `CoreModeHudUI.cs`（手続き生成Canvas/sortingOrder48000）。上部左=自コアHPバー・上部右=敵コアHPバー（Filled Image＋現/最大HP、LateUpdateで `HealthRatio`/`CurrentHealth` 反映）・中央上部=フェーズ＋ウェーブ番号。`SpawnCores`(初期)・`StartCoreWave`(戦闘中)・`CoreAutoAdvanceRoutine`(小休止/編成カウントダウン)から配線。③Phase2でフェーズ告知に使った `ScorePopupUI.Show(0,…)` は amount==0 で非表示だった不具合を HUD 置換で解消。Compilation OK（CSエラー0）。**実機の通し確認・balance（コアHP/ウェーブ強度/区切り間隔）は要手動調整**。これで R2-coremode は Phase1-3 完了、残はバランス調整のみ。[DESIGN_R2-coremode.md] Phase3 記録追記。
- 2026-06-02: **図鑑ボス詳細が開かない不具合の修正**（Claude/Cowork+Unity MCP）。ロビーの図鑑で解放済みボスをクリックしても `UnitStatusPanelUI` が表示されなかった（ButtonJuiceのDOTweenだけ動く）。実機調査で根因2点を特定：①`UnitStatusPanelUI` のCanvas `sortingOrder=50020` が **Unityの16bit short上限(32767)を超えて桁あふれ→-15516** に化け、ロビーの背景キャンバス(order0)の後ろへ回り非表示（同様にLobbyUI=60050→-5486等も判明＝潜在）。→ 32760 に修正し最前面化。②`PlayPanelAppear` の `CanvasGroup.DOFade(0→1)` がロビーでは進まず **alpha=0で張り付き**。→ alphaは即1で表示を担保し、Tweenはスケールのポップのみ＋OnKillでalpha=1保証に変更。プレイモードで `active=True/alpha=1/order=32760` を確認。Compilation OK（CSエラー0）。
- 2026-06-02: **UIキャンバスの sortingOrder 桁あふれを全面修正**（Claude/Cowork+Unity MCP）。Unityの `Canvas.sortingOrder` は16bit short（[-32768,32767]）で、プロジェクトの多くのUIが 48000〜61000 を設定しており桁あふれして負値（例:50020→-15516, 60050→-5486）に化けていた（図鑑詳細が背景の裏に回り非表示だった根因）。相対順序を保ったまま short 範囲内へ再マップ：CoreModeHud=13000 / ItemBench=13040 / AugmentHud=SynergyPanel=14000 / AugmentTooltip=ScorePopup=14500 / ItemTooltip=15000 / SynergyTooltip=15005 / CoinIncome=15010 / Localization=Options=25000 / ChapterRoster=25010 / BossReward=25020 / Lobby=25050 / Result=25500 / AugmentSelection=Settings=26000。UnitStatusPanel（詳細パネル）は図鑑(LobbyUI=25050)やHUDより前面に出す必要があるため最前面 32000 に設定。プレイモードで全キャンバスが正値（負値0件）になり、詳細パネルが alpha=1・order=32000 で最前面表示されることを確認。Compilation OK（CSエラー0）。※ワールドのSpriteRenderer系（Draggable/ItemInstance/Healthbar/Tile/Effect等）は値が32767未満かつ別ソートで競合しないため不変。
- 2026-06-02: **図鑑ボス詳細を「全画面の図鑑専用ビュー」へ刷新**（Claude/Cowork+Unity MCP）。従来はゲーム中の右側 `UnitStatusPanelUI` を流用していたが、図鑑からのクリックを `LobbyUI.collectionDetailView`（新規・全画面）へ差し替え。レイアウト：左に大サイズのボス（待機アニメ）＋名前(JA/EN)＋コスト＋第◯章タグ＋シナジーチップ、右に育成(Lv・ステータス+%・獲得回数)／ステータス(HP・ATKは育成補正込み、攻速・射程・被ダメ軽減・マナ)／固有スキル(名前＋説明)／紹介文。`‹ ›` で所持ボス間を前後ナビ、戻るで一覧へ。大スプライトは盤外でプレハブを待機アニメ再生させ `SpriteRenderer.sprite` を毎フレーム UI Image へ写す新コンポーネント `CollectionBossAnimator`（左右反転も反映）。離脱時に生成実体を破棄。実装の再利用：`UnitStatusPanelUI.GetSkillTitleFor/GetSkillBodyFor`（既存スキル文生成の公開ラッパー新設）、`LocalizationManager.BossFlavor`（13体のJA/EN紹介文を新規追加）。ステータスは公開フィールド(baseDamage/range/attackSpeed)＋MaxHealth/MaxMana/DamageReduction を参照。Compilation OK（CSエラー0）。実機の表示・アニメ・ナビは要手動確認（MCP eval が重い反射呼び出しの戻り値を落とすため自動確認は限定的、例外ログは0件）。[DESIGN_R1-collection.md] 追補。
- 2026-06-02: **図鑑詳細に「ショップアイコンの大表示」と「クリックで戦闘アクション再生」を追加**（Claude/Cowork）。①表示切替タブ（アニメ／アイコン）を枠上に追加：`SetCollectionDetailMode` でスプライト(待機アニメ)とショップアイコン(EntityData.icon)の大表示を切替（アイコン時はミラー停止）。②大スプライトを Button 化し、クリックで `BaseEntity.PlayPreviewAction(ability)` を呼んで攻撃→スキルを交互に1回再生（クリップ長後に待機へ復帰、戦闘ロジック非干渉）。BaseEntity に `HasAbilityAnimation` と `PlayPreviewAction`/`PreviewActionReturn` を新設（既存の BeginActionAnimation/EndActionAnimation・animatorトリガ attack/ability を流用）。Compilation OK（CSエラー0）。実機の見た目・アニメ再生は要手動確認。[DESIGN_R1-collection.md] 追補。
- 2026-06-02: **図鑑詳細：モーション巡回＋スプライト拡大＋アイコン全画面**（Claude/Cowork）。①スプライトをクリックするたび 待機→移動→攻撃→スキル→死亡 を巡回再生。BaseEntity に `PreviewSetMotion(idle/walk/attack/ability/death)`（毎回 animator.Rebind で待機へ戻してから再生＝死亡後も復帰可）と `HasWalkAnimation`/`HasDeathAnimation` を追加。利用可能なモーションのみ巡回リスト化（walk/ability/death は所持時のみ）。ヒントに現在モーション名を表示。②大スプライトをホイールスクロールで枠内拡大（新規 `CollectionZoom : IScrollHandler`、1〜3倍、flipX符号維持）。枠に RectMask2D を付与し拡大分をクリップ。③ショップアイコンをクリックで全画面拡大オーバーレイ（`cdIconOverlay`／クリックで閉じる）。アイコンをButton化し、アニメ/アイコンの表示切替はタブで継続。ボス切替・再表示時に拡大とオーバーレイをリセット。Compilation OK（CSエラー0）。実機の見た目・各モーション再生・拡大操作は要手動確認。[DESIGN_R1-collection.md] 追補。
- 2026-06-02: **盤面ギミックのチャプター進行制御＋フォーメーションの可視化**（Claude/Cowork）。①転がる巨大物（③b）を **チャプター3以降のみ**に変更（ch1-2は素のユニット戦）。`MidBossHazardRoutine` の出現回数をチャプターでスケール（ch3-4=2／ch5-6=3／ch7+=4）。コア戦では出さない。②配置フォーメーション（②）の可視化：編成中（戦闘外）に `GameManager.UpdateFormationPreview` が横3/縦3を検出し、成立マスを色付きマーカー（横=橙／縦=青／重複=紫、手続き生成スプライト・sortingOrder300）でライブ表示。新規 `FormationHintUI`（画面下部中央・sortingOrder14200）で「横3=突撃／縦3=鉄壁（揃うとマスが光る）」のガイドと、成立中は隊形名＋効果を表示（初見ユーザー向け）。戦闘開始時 `ApplyFormationBonuses` で各対象ユニットに `PlayAreaIndicator` の発動エフェクトを付与し、編成マーカーは消去。Compilation OK（CSエラー0）。実機の見た目・発光・ガイド表示は要手動確認。今後：チャプター進行に応じた「マスの変化（穴/不可侵など idea③a）」は別タスクで追加予定。[DESIGN_board-gimmicks.md] 追補。
- 2026-06-02: **フォーメーション誤検出の修正＋種類追加**（Claude/Cowork）。①誤検出修正：行/列の判定を `worldPosition` の `RoundToInt` から、GridManager のクラスタ列/行インデックス（`GetBoardColumn`/`GetBoardRow`、x/yの近接クラスタにスナップ。`InitializeNodeColumns` に rowByNode を追加）へ変更。グリッド間隔が整数でない場合に隣接マスが同一に丸められ、スターアップ後の配置で意図せず隊形成立していた問題を解消。②検出を `DetectFormations`（盤面プレイヤー配置→列/行/2x2/斜めを判定）に一本化し、プレビュー（マーカー色＋ガイド）と戦闘開始バフ（`ApplyFormationBonuses`）で共用。③フォーメーション種類を4種に拡張：突撃（横3・橙）／鉄壁（縦3・青）／方陣（2×2・緑＝与ダメ+8%/被ダメ-8%/シールド8%）／楔（斜め3・マゼンタ＝与ダメ+25%/攻撃速度+10%）。ガイド文言・マーカー色・成立中表示も4種対応。Compilation OK（CSエラー0）。実機での誤検出解消・新フォーメーションの見た目/効果は要手動確認。balanceはR3。[DESIGN_board-gimmicks.md] 追補。
- 2026-06-02: **陣形ガイドをテキスト→パネル化**（Claude/Cowork）。ショップ上の1行テキスト表示を廃し、`FormationHintUI` を陣形ガイドパネルに作り替え。画面左中央に4陣形（突撃/鉄壁/方陣/楔）を「3×3ミニ盤面の例（成立セルを色付け）＋名称＋効果」で一覧表示。編成中のみ表示、成立中の陣形は行背景ハイライト＋「● 発動中」タグ。ヘッダーの−/＋で折りたたみ可。GameManager は `SetBuildPhase`/`SetActiveFormations` で駆動。Compilation OK（CSエラー0）。実機の見た目・折りたたみ・成立ハイライトは要手動確認。[DESIGN_board-gimmicks.md] 追補。
- 2026-06-02: **陣形ガイドパネルの再配置**（Claude/Cowork）。左のシナジー/オーグメントHUDと重なっていたため、パネルを画面右上（オプション直下）へ移動し既定で折りたたみ（ヘッダー「陣形ガイド ＋」のみ）に変更。ヘッダーのクリックで下方向に展開し、4陣形の例＋効果を表示。展開時も左HUD・ショップ・ラウンド表示と非干渉。Compilation OK（CSエラー0）。
- 2026-06-02: **ユニット詳細パネルの重なり解消**（Claude/Cowork）。詳細パネル(UnitStatusPanelUI)が右上のオプションボタンと陣形ガイドを覆っていた件を修正。①詳細パネルの上端を TopReserve=64 分だけ下げ（anchoredPosition.y、usableHeight も減算、最小高480に）、右上オプションを隠さないように。②`UnitStatusPanelUI.IsOpen`（公開）を追加し、GameManager の `UpdateFormationPreview` で詳細パネル表示中は陣形ガイドパネルを隠す（マスの発光は継続）。Compilation OK（CSエラー0）。実機での重なり解消は要手動確認。
- 2026-06-02: **詳細パネルのFIGHT重なり解消＋陣形ガイドの共存改善**（Claude/Cowork）。①UnitStatusPanelUI を縮小して右下FIGHTを避ける：PanelVisualScale 1.2→1.1 / MaxPanelHeight 650→580 / TopReserve 64→92 / BottomUiReserve 124→150、usableHeight をスケールで割って実視覚高さで上下に収める。上はオプション＋陣形バー、下はFIGHT＋ショップを回避。②陣形ガイドは「ヘッダーのバーは常時表示」に戻し、詳細パネル表示中は隠さず `ForceCollapse()` で折りたたむだけ（バーは見える／展開時の重なりは回避）。GameManager は常時 `SetBuildPhase(true)`、詳細パネル表示中のみ `ForceCollapse`。Compilation OK（CSエラー0）。実機での重なり解消は要手動確認。
- 2026-06-02: **詳細パネルの内容はみ出し修正＋陣形ガイドを左上へ移設**（Claude/Cowork）。①UnitStatusPanelUI：アイテム枠が y≈-642 まであるのに最大高580で見切れていた件を、固定コンテンツ高 ContentHeight=660（全要素が収まる）＋利用可能高に合わせた全体スケール（`scale=Clamp(availableVisual/660,0.6,1.0)`、anchoredPosition.y=-(margin+TopReserve)）に変更。これで全情報がパネル内に収まり、上はオプション・下はFIGHT/ショップを回避。PlayPanelAppear はスケール競合回避のため alpha フェードのみに。②陣形ガイド：右側は詳細パネルと競合するため、ウェーブ進捗（ラウンド表示）の左＝画面上部左寄り(anchor左上, x=232)へ移設。詳細パネルとの自動折りたたみ連携は不要になったため撤去（TopReserve も 92→60 に）。Compilation OK（CSエラー0）。実機での収まり・配置は要手動確認。
- 2026-06-02: **陣形ガイドをトグルボタン＋中央ポップアップ方式へ**（Claude/Cowork）。常時展開パネルがHUD（左の系統リスト／ウェーブ進捗バー／右の詳細パネル）と被る問題を、ウェーブ進捗の左に小さなトグルボタン（「▣ 陣形ガイド ▾」幅190・隙間に収まる）だけを常時置き、押すと盤面中央に4陣形の一覧ポップアップ（形の例＋効果）を表示する方式に変更。成立中はトグルボタンに「（発動中 n）」表示＋色変化、ポップアップ各行もハイライト。編成中のみボタン表示、戦闘/選択中は隠す。マスの発光は従来どおり継続。あわせて全体HUDレイアウト案をユーザーへmockupで提示。Compilation OK（CSエラー0）。実機での収まり確認は手動。
- 2026-06-03: **チャプター3 追加**（Claude/Cowork）。`BuildChapter3Rounds()` を新規実装し `BuildChapterRounds` の `case 3` に配線。ch2 を雛形に一段難しくした 4ステージ/全33ラウンド構成（Stage1=肩慣らし3／Stage2=cost2-3・中ボス2-5,2-10／Stage3=cost3-4・中ボス3-5,3-10／Stage4=cost4-5・中ボス4-7/8/9・章ボス4-10）。章ボス 4-10＝報酬 cost4 **Maehvmk(★3)**（撃破で恒久解放）＋cost5大護衛(Gol/Invader/Kron/Skyfalltyrant)。中ボスは `RecruitMidBoss`（同コスト3体→1体を章内解放）。盤面ギミック「転がる巨大物」は既存ゲート（`currentChapter>=3 && IsMidBossWave`）によりch3の中ボス戦で自動的に出る（ch1・2は素戦）。`LobbyUI.PlayableChapterCount` 2→3（章2クリアで解放、`IsChapterUnlocked` は汎用対応）。章カード素材は Play/Chapter に12枚あり3章ぶん表示可。Compilation OK（CSエラー0）。残: 実機1周・難易度/数値調整（R3-balance）。[DESIGN_chapter3.md] 起票＋実装。
- 2026-06-03: **章3が解放表示されない不具合の修正**（Claude/Cowork）。解放判定（`IsChapterUnlocked(3)`＝章2クリア）はバックエンドで正常（実機で ch2cleared=True / unlock3=True を確認）だったが、ロビーの章カードが `chapterCardsBuilt` フラグで一度しか生成されず、章2クリア後も古い「ロック」状態のまま表示されていた。`BuildChapterCards` を「開くたびに既存カードを破棄して作り直す」方式に変更し、最新の解放状況を常に反映するようにした。Compilation OK（CSエラー0）。
- 2026-06-03: **序盤難易度の是正（A＋C）**（Claude/Cowork、ユーザー方針決定）。後半章のステージ1-1から敵3体出て1体スタートでは確定負けだった問題に対応。【A】全章ステージ1の立ち上がりを共通でやさしく固定：1-1=敵1体／1-2=敵2体（ch2・ch3を緩和、ch1は元から1体）。難度差はステージ2以降で表現。【C】章スケールの開始リソースを追加：`PlayerData.PendingStartExtraLevels/Money`（ResetEconomyForNewRunで適用）＋GameManager開始処理で章に応じ tier=clamp(章-1,0,2) を付与（ch2=+1Lv/+3G/初期2体、ch3=+2Lv/+6G/初期3体。配置上限=Levelなので後半章は複数体置ける）。コア戦は対象外。Compilation OK（CSエラー0）。実機で“確定負け”解消・後半章の立ち上がり手応えは要確認、数値はR3-balance。
- 2026-06-03: **中ボス報酬の刷新＋強化マス（新規）**（Claude/Cowork、ユーザー仕様）。キャラ解放頻度が高い問題に対し報酬を多様化。全章共通スケジュール `ApplyMidBossRewardSchedule`：2-5=アイテム3択 / 2-10=ボス選択(cost3) / 3-5=強化マス / 3-10=ボス選択(cost4) / 4-5=ボス選択×2 / 4-7・4-8=アイテム3択 / 4-9=大量コイン(40+(章-1)*20)。`WaveDefinition` に `RewardKind/RewardCount/RewardCoins`、`CompleteCurrentWave` で分岐。ボス選択は `ShowMidBossRecruit(def,count)` 連鎖で複数選択対応。アイテム3択＝新規 `ItemRewardSelectionUI`（防具/攻撃/秘力から各1ランダム→`ReturnItemToBench`）。コインは `GrantCoinReward`。【新規】強化マス：`BuffTileRewardUI` で種別（攻撃/防御/秘力）選択→盤面の自陣マスをクリックで設置（`HandleBuffTilePlacementClick`、GetTileAtWorldPosition→GetNodeForTile→自陣判定）。`buffTiles`(Node+種別) を**チャプター中永続・累積**で保持し、`ApplyBuffTileBonuses` を戦闘開始時に適用（攻=与ダメ+30%/攻速×1.15、防=被ダメ-20%/シールド25%、秘=攻速×1.25/与ダメ+10%）＋色付きマーカー常設。コア戦は対象外。ボス解放は2-10/3-10/4-5のみに減少（コスト上限解放は全中ボス継続）。Compilation OK（CSエラー0）。実機の各UI表示・強化マスのクリック設置/適用は要手動確認、数値はR3。[DESIGN_R2-rewards.md] 起票＋実装。
- 2026-06-03: **1-3緩和＋倍速のチャプター持続**（Claude/Cowork）。①ステージ1-3の敵を緩和：ch2=コスト2近接を外しコスト1中心の3体／ch3=コスト2近接2＋Shadowlord★2をやめコスト1×2＋コスト2遠の3体に。立ち上がりの過負荷を解消。②倍速がステージ跨ぎで1倍に戻る不具合を修正：`OptionsPanelUI.DesiredGameSpeed`（PlayerPrefs/インスタンス由来の設定速度）を公開し、`ResultPanelUI`/`AugmentSelectionUI` のポーズ解除復帰を「捕捉した previousTimeScale」から `DesiredGameSpeed` 復帰に変更。これでステージ結果やオーグメント選択を挟んでも倍速がチャプター全体で維持される（章クリア→ロビーは従来どおり1倍）。Compilation OK（CSエラー0）。
- 2026-06-03: **ウェーブ勝利の祝福インターバル**（Claude/Cowork）。勝利後に即配置が戻る問題を改善。新規 `WaveClearBannerUI`（中央に「ウェーブクリア！/中ボス撃破！/ボス撃破！」＋ランダム称賛文、スケールポップ＋フェードの演出、SetUpdate(true)で倍速/ポーズ非依存）。`GameManager.BeginWaveClearCelebration()`＝勝利検出時に `IsRoundInProgress=false` で戦闘を止め（味方は待機・配置はまだ戻さない）バナー表示→`WaveClearCelebrationRoutine`（リアル時間 約1.9秒、クリックで早送り可）後に従来の `CompleteCurrentWave`（配置復元/収入/報酬）へ。`TryEndRound`/`HandleRoundTimeout` の勝利路を本演出経由に変更。演出中は `DebugFight`(FIGHT)を無効化。デバッグ訓練ウェーブは演出スキップ、コア戦は独自フロー（対象外）。Compilation OK（CSエラー0）。
- 2026-06-03: **途中リザルト抑制＋アイテム取り外し機**（Claude/Cowork、ユーザー仕様）。①ステージ1〜3の途中リザルトを表示しないよう `QueueStageResult` の非・章クリア分岐を削除（スコア集計 chapterStageScores/Times は維持）。最終ステージ4クリア（章クリア）時のみリザルト表示。②アイテム取り外し機を新規追加：`ItemData.isRemover`＋`ItemData.Remover()`（消費されない特殊ツール、icon=repairstaff流用）。`GameManager.EnsureRemoverToolInBench()` でラン開始時にアイテムベンチへ常設。`TryEquipItemToEntity` 冒頭で remover を判定し、装備中の味方に重ねると `RemoveAllItems`→`ReturnItemsToBench` でアイテムを外して戻す（取り外し機自身は消費せずベンチへ復帰）。何度でも使用可。Catalog 非掲載のため報酬/ショップには出ない。Compilation OK（CSエラー0）。実機での表示・取り外し動作は要確認。
- 2026-06-03: **おすすめ3機能を実装（ボス固有ギミック／ヒーロー必殺／仲間化ボス育成パッシブ）**（Claude/Cowork）。①章ボス固有ギミック：`BossMechanicRoutine`＋`BossAoeStrike`＝章ボス戦で味方を狙う予兆付き範囲攻撃を一定間隔（2+章 回）落とす（半径1.6マス、110+章*60ダメ＋0.6sスタン）。DebugFightのボス戦開始でトリガー、コア戦除外。②ヒーロー＋必殺：`heroUnitId`(既定Wolfpunch)を毎ラン初手に確定付与（`GrantHeroUnit`）。`UseHeroUltimate`＝1戦1回、盤面の味方全体へシールド20%/与ダメ+25%/攻速×1.2(6s)。新規 `HeroUltButtonUI`（右下・戦闘中のみ・使用後グレー）。`heroUltUsedThisWave` を戦闘開始でリセット。③仲間化ボス育成パッシブ：`ApplyRecruitedBossPassives`（戦闘開始時）＝解放ボスにアフィニティ節目で Lv3開幕シールド15%/Lv5攻速×1.12/Lv8与ダメ+12%。図鑑詳細の育成パネルに節目（●/○）表示。Compilation OK（CSエラー0）。実機の体感・数値・ボス個別化は R3。[DESIGN_R3-bossfeel.md] 起票＋実装。
- 2026-06-03: **役割の切り替え＋引き継ぎ整備**（ユーザー指示）。以降 Cowork(Claude) は設計・レビュー専任、実装/シーン編集/実機テスト/コミットは Claude Code 担当（COLLAB_PROTOCOL の設計・実装分離へ回帰）。今セッションで Cowork が直接実装した機能群（章3・報酬刷新・強化マス・勝利演出・取り外し機・ボス固有ギミック・ヒーロー必殺・育成パッシブ 等）は「コンパイル0だが実機未検証」。`docs/CLAUDE_HANDOFF.md` §9 に検証チェックリスト（受け入れ基準・触ったファイル・リスク）を追記。Claude Code が実機検証・数値/レイアウト調整・コミットを行う。
- 2026-06-04: **ヒーロー専用3体＋ヒーロー選択の設計起票**（Claude/Cowork、設計専任体制での初タスク）。ユーザー提供の f1/f2/f6_general（過去にcost4用で削除した将官シート＝ヒーロー用途なら被り問題なし）を主人公専用ユニット3体（仮: アルディン=守護/カガチ=攻撃/ヴェスナ=秘力、各専用必殺つき）として実装する [DESIGN_R3-hero-units.md] と、新規セーブ作成時に3体から選択して `SaveData.heroUnitId` に保存し `GrantHeroUnit`/`UseHeroUltimate` を選択IDで分岐させる [DESIGN_R3-hero-select.md] を起票。排他制御（ショップ/敵ウェーブ/召喚/報酬/売却から除外）と旧セーブの後方互換（未選択なら一度だけ選択画面）を明記。実装は Claude Code（hero-units → hero-select の順）。
- 2026-06-05: **ヒーロー選択画面の一枚絵アセット準備＋設計更新**（Claude/Cowork、設計・素材専任）。ユーザー指定によりヒーロー選択カードの顔を一枚絵イラストに決定。reference/duelyst の `generals/general_f1/f2/f6@2x.png`（5120×3700・透過あり）を透過バウンディングボックスでトリミング＋最大辺1600pxへ縮小し、`Assets/Resources/UI/HeroSelect/hero_aldin.png / hero_kagachi.png / hero_vesna.png` として配置（`Resources.Load<Sprite>("UI/HeroSelect/…")` で取得可）。[DESIGN_R3-hero-select.md] の選択画面仕様を一枚絵レイアウトへ改訂（CollectionBossAnimator のアニメ転写は不要に＝盤外プレハブの後始末リスク削減、preserveAspect 必須、import=Sprite/MipMaps OFF 確認を実装側チェックに追加）。実装は引き続き Claude Code。
- 2026-06-06: **役割を実装担当へ戻す＋ヒーロー機能の実機検証（Cowork+Unity MCP）**。ユーザー指示で Cowork が再び MCP で実装/検証を担当。①既に Claude Code が hero-units（HeroAldin/Kagachi/Vesna を DB登録＋prefab/anim5種/sprite/icon フルパイプライン）と hero-select（SaveData.heroUnitId＋SaveManager Get/Set、LobbyUI.SelectSlot→未選択ならShowHeroSelect割り込み→ConfirmHeroSelection保存、BuildHeroCard が `Resources/UI/HeroArt/<id>` の立ち絵を preserveAspect 表示）を実装済みと確認。排他制御は中央フィルタ `IsLegionOnlySummonData` にヒーローを含める形＋売却ガード（TrySellEntity）＋静的プール(LockedCost3Pool/Cost4Pool/ChapterBossRewardUnitIds)に非混入で7点とも担保。必殺は UseHeroUltimate が heroUnitId で3分岐（聖盾/修羅/蒼炎）、HeroUltButtonUI のラベルも GetHeroUltimateName 追従。②実機検証：プレイモード(LobbyScene)で例外0、ShowHeroSelect 構築後に立ち絵3枚へ HeroArt スプライト(2048×1480)が正しく入る（HeroVesna/Kagachi/Aldin）ことを確認＝ユーザー提供イラストの表示OK。③前ターンに Cowork が置いた孤児 `Assets/Resources/UI/HeroSelect/`（コード未参照）を削除。コンパイル0。残：図鑑モーション巡回・強化マス設置はクリック実操作の手動確認、各数値は R3-balance。
- 2026-06-06: **ショップスロットのアイコン/フレームサイズをユーザー指定へ固定**（Cowork+Unity MCP）。`UICard` のランタイム補正が frame 高さを `frameTargetHeight=140` に強制していたため、シーンで 150 にしても実機で 140 に戻る問題を修正。アイコン/フレームを指定サイズへ確実に揃えるよう公開フィールド `iconTargetSize=(255,140)` / `frameTargetWidth=255` を追加し、`frameTargetHeight` を 150 に変更。`EnsureFrameReference` で icon.sizeDelta=iconTargetSize、frame.sizeDelta=(frameTargetWidth,frameTargetHeight) を1回だけ適用（縦位置 frameVerticalOffset は据え置き）。これら新/変更フィールドは Slot.prefab・GameScene 双方で未シリアライズ＝ロード時にコード既定値が効くため YAML 編集不要。Slot.prefab を生成して初期化を走らせ icon=255×140 / frame=255×150 を実測確認。コンパイル0。
- 2026-06-06: **アイテム3択報酬UIの見栄え刷新**（Cowork+Unity MCP）。`ItemRewardSelectionUI` を単色四角＋文字詰め込みから素材ベースへ作り替え。パネル背景=`UI/Augment/card_panel`(9-slice)＋タイトル帯。カードはカテゴリ別のカード枠スプライト（防具=`UI/Cards/silver_front`／攻撃=`gold_front`／秘力=`prism_front`）を背景に、アイコンを`UI/ItemBench/artifact_frame`（カテゴリ色）で囲み中央上に配置、名前＋効果は暗幕スクリム上に載せて可読性を確保（busyなカード絵の上でも読める）。出現スケールポップ＋ホバー拡大(CardHover/IPointer*)演出、選択はカード全体クリック。カードサイズ280×384・パネル980×600。参照スプライトは全て Resources.Load=True を実機確認。コンパイル0。※最終的な見た目の微調整は実機目視（MCPはエディタ非フォーカス時にスクショ書き出し不可）。
- 2026-06-07: **アイテム3択UIの「小さく窮屈」を修正＋オーグメント選択に寄せて拡大**（Cowork+Unity MCP）。前版が小さく寄っていた根因は、`HorizontalLayoutGroup` の `childControlWidth=false` のため `LayoutElement.preferredWidth` が無視されカードが既定サイズで並んでいたこと。AugmentSelectionUI 同様の「絶対配置＋明示サイズ」へ変更：カード320×460を x=-370/0/370 に配置、パネル1180×660、タイトル36pt帯。アイコン枠168px・アイコン118px、名前22pt・効果16pt、スクリムをカード幅いっぱい近くに広げて余白を確保。CanvasScaler を ConstantPixelSize→ScaleWithScreenSize(1920×1080) にして解像度差でも寸法維持。コンパイル0。※最終見た目はエディタ非フォーカス時にMCPがスクショ書き出し不可のため実機目視推奨。
- 2026-06-07: **強化マス設置でマスを選べない不具合を修正**（Cowork+Unity MCP）。種別選択後の「盤面をクリック」で、どのマスを押しても設置できなかった根因は、`HandleBuffTilePlacementClick` が `cam.ScreenToWorldPoint(Input.mousePosition)`（z=0のまま）で変換していたこと。盤面はパースペクティブ表示のため、z=0のスクリーン点は近接面に落ち盤面に当たらず、`GetTileAtWorldPosition` が常に null を返していた。動作実績のある `Draggable.GetPointerWorldPosition` と同方式に修正：スクリーン点zに「カメラから盤面(z=0)までの距離」=`Mathf.Abs(cam.transform.position.z)` を与えてから変換し、取れない場合は `Physics2D.OverlapPoint` でコライダー探索するフォールバックも追加。自陣判定（IsDeploymentNode Team1）は据え置き。コンパイル0。残：実機で自陣マスに設置できること・敵陣は弾くことの確認。
- 2026-06-07: **ボス報酬選択が5枠はみ出し＋進行不能になる不具合を修正**（Cowork+Unity MCP）。複数選択（4-5のボス×2）で1体選ぶと `BossRewardSelectionUI.Show` を再呼び出しするが、`ClearOptions` の `Destroy` がフレーム末まで遅延するため「破棄待ちの旧3枚＋新2枚＝5枚」が同フレームに並んで枠をはみ出し、さらに破棄待ちの旧カード(Button)が次クリックを奪って `bossRewardSelectionPending` が解除されず進行不能（FIGHTで「Choose a boss reward...」警告）になっていた。修正：`ClearOptions` で破棄前に `SetActive(false)`＋`SetParent(null)` し、表示とレイアウトと当たり判定から即時除外（次フレームの実破棄を待たない）。同パターンの `ItemRewardSelectionUI.ClearOptions` も同様に統一。あわせて選択パネルを 820→900px に広げ3カード(240×3＋spacing)が収まるように。コンパイル0。残：実機で単一/複数ボス選択が3枚に収まり、選択後に次戦へ進めることの確認。
- 2026-06-07: **ボス選択が5択になり進行不能になる根因を修正**（Cowork+Unity MCP）。実機で「仲間にするボスを選択」に5体（Cinderwraith/Draugarlord/Kingsguard/Dissonance/Wujin）が並んだ件。根因は `ShowMidBossRecruit` が、対象ウェーブに `RecruitCandidateIds` が無い場合に `Cost4Pool` 全体へフォールバックし、未解放分（5体以上）をそのまま候補化していたこと（報酬スケジュールは 4-5 に Recruit×2 を割り当てるが、その 4-5 ウェーブは RecruitMidBoss 生成ではなく候補ID未設定）。修正：`pendingRecruitOptions` を常に最大3に制限（超過分を切り捨て）。これで枠はみ出しと、はみ出したカードによるクリック不能＝進行不能を解消。前修正（ClearOptions 即時除外・パネル900px）と合わせて、単一/複数(×2)選択とも3枚以内で完結する。コンパイル0。残：実機で 4-5 のボス×2 を2体選んで次戦へ進めることの確認。
- 2026-06-07: **デバッグメニューに検証用6機能を追加**（Cowork+Unity MCP）。`DebugMenu`(F8, #if UNITY_EDITOR||DEVELOPMENT_BUILD) に GameManager のデバッグAPIを配線：①ラウンドジャンプ（現在チャプターの全ラウンドをラベル付き一覧表示し、押すと `currentWaveIndex` を設定＝次FIGHTでそのラウンド開始。`DebugJumpToWave`/`DebugWaveLabel`/`DebugWaveCount`/`DebugCurrentWaveNumber`）②アイテム選択強制表示（`DebugForceItemReward`→`ShowItemChoice3Reward`）③強化タイル選択強制表示（`DebugForceBuffTileReward`→`ShowBuffTileReward`）④即勝利（`DebugInstantWinRound`＝`DebugFight`で開始→`ClearEnemyUnits`→`TryEndRound`で通常の勝利フロー）⑤味方無敵 ⑥敵無敵（`DebugPlayerInvincible`/`DebugEnemyInvincible` を `BaseEntity.TakeDamage` 冒頭で参照しダメージ無効化）。全API・無敵チェックとも `#if UNITY_EDITOR||DEVELOPMENT_BUILD` でガードし製品ビルドからは除外。コンパイル0。
- 2026-06-07: **4-5のボス×2選択後に進行不能になる真因を修正**（Cowork+Unity MCP）。前回の候補3制限でも残っていた進行不能の根因は `BossRewardSelectionUI.SelectOption` の処理順。`onSelected.Invoke(...)` の後に `Hide()` していたため、複数選択(4-5=Recruit×2)で1体目を選ぶと、コールバック `OnMidBossRecruit` が2体目用に `Show()`(SetActive true) した直後に `Hide()` がそれを閉じてしまい、UIが消えたまま `bossRewardSelectionPending` だけ true で残って次戦(4-6)のFIGHTが弾かれていた。修正：SelectOption を「コールバックを退避→先に Hide()→その後 Invoke」に変更し、コールバックが必要に応じて再表示できるようにした。単一選択(章ボス/2-10等)も Hide→pending解除の順で正常。コンパイル0。残：実機で4-5の2体選択→4-6開始の確認。
- 2026-06-07: **中ボス仲間化を「戦ったボスから1体選択」に統一（4-5偽ボス廃止）**（Cowork+Unity MCP、ユーザー方針）。調査の結果、2-10/3-10 の `RecruitMidBoss` は元々「ボス3体と戦い→その3体から1体仲間化」で正しく、ズレていたのは報酬スケジュールが 4-5（普通の戦闘）を無理やり中ボス化し Cost4Pool から無関係ボスを出していた点のみと判明。修正：①`ApplyMidBossRewardSchedule` の `4-5 Recruit×2` を廃止（4-5は通常戦闘へ）。stage4 の仲間化は実ボス戦 `4-8`（RecruitMidBoss）に Recruit×1 で割当。仲間化は 2-10/3-10/4-8 の3か所＝全て「ボス3体出現→1体選択」。②`ShowMidBossRecruit` の Cost4Pool フォールバックを廃止し、候補は必ず「そのウェーブで戦った敵の UnitId」から導出（RecruitCandidateIds 優先）＝戦っていないボスが並ぶことが原理的に無くなる。中ボスは常に1体選択（×2廃止）。章ボスは従来どおり「戦った章ボス本体を恒久解放」で既に一致（ch3=Maehvmk 等）。各章の RecruitMidBoss は章ごとに別ボスIDを指定済み＝新規アート不要で章別の顔ぶれが出る。コンパイル0。残：実機で 2-10/3-10/4-8 が戦闘に出たボスのみを候補に出すこと・1体選択で次戦に進めることの確認（DebugメニューのRound Jump＋即勝利で確認可）。
- 2026-06-07: **報酬UIの見た目を3種で統一＋コード小掃除**（Cowork+Unity MCP）。①ボス選択(`BossRewardSelectionUI`)と強化マス(`BuffTileRewardUI`)を、先に刷新したアイテム3択UIと同じ見た目へ統一：パネル背景=`UI/Augment/card_panel`(9-slice)＋タイトル帯、カードは大型化＋絶対配置、出現スケールポップ＋ホバー拡大。強化マスは種別カードを防御=silver/攻撃=gold/秘力=prism のカード枠＋`artifact_frame`スウォッチ＋効果文をスクリム表示に。②小掃除：未使用化した `LockedCost3Pool`/`Cost4Pool` を削除、章1の中ボスコメントを実際の出現ボス＆新報酬に修正。コンパイル0・プレイモードで例外0を確認。各UIの最終的な見た目と、戦闘内機能（陣形/コア戦/取り外し機/ヒーロー必殺等）の体感は、追加済みデバッグ機能（Round Jump/即勝利/報酬強制/無敵）での実プレイ確認推奨。
- 2026-06-07: **ゲームオーバーのリザルト＋ロビー復帰／章クリアのボス解放・強化演出**（Cowork+Unity MCP、ユーザー要望）。①ゲームオーバー：`ResultPanelUI.ShowGameOver(score,time,subtitle)` を追加（タイトル赤「ゲームオーバー」＋到達タイム/スコア＋「ロビーへ戻る」）。`OnContinueClicked` を章クリア/ゲームオーバー両対応にし、閉じると `RequestReturnToLobby()`。`TriggerGameOver` から到達ラウンド（コア戦は「コアが破壊された」）を添えて表示。チャプター戦・コア戦とも敗北でリザルト→ロビーに戻れる。②章クリア演出：新規 `BossUnlockBannerUI`（独立Canvas/sortingOrder25600・ポーズ非依存DOTween・クリック早送り/2.6s自動）。章クリア処理で `HasBossAlly` により初回判定→`AddBossAlly`→`GetBossAffinityLevel` を取得し、初回=「★ボス解放！」（金・解放文）、2回目以降=「★ボス強化！ Lv.X（ステータス+N%）」（青）をボスアイコン＋グロー＋ポップで表示。強化ロジックは既存のアフィニティ(recruitCount++で+6%/Lv)を流用。`GetEntityIconById` ヘルパ追加。コンパイル0／プレイモードで例外0。実機の見た目・遷移はプレイ確認推奨（DebugのRound Jumpで4-10→即勝利で章クリア演出、戦闘で敗北→ゲームオーバー確認）。
- 2026-06-07: **シナジー+1オーグメントが発動に反映されない不具合＋陣形マーカーが敵陣に出る不具合を修正**（Cowork+Unity MCP、ユーザー報告）。①シナジー：`CountSynergiesForTeam` の発動段階(`activeTiers`)は生の盤面カウントのみで計算され、オーグメント加算（戦士/狙撃/秘術エンブレム＝AugmentSynergyBonus*、戦闘中ランダム+1＝AdditionalSynergyBonusThisCombat）は表示用 `GetSynergyCount` にしか入っていなかった。そのため「戦士の真髄」等で表示が4でも発動段階0＝効果未発動だった。共通ヘルパ `PlayerSynergyAugmentBonus(type)` を新設し、表示カウントと発動段階の両方で同じ加算を使うよう統一（プレイヤーチームのみ）。prism_all_synergy は従来どおり counts 側で加算済み。②陣形マーカー：勝利演出中（IsRoundInProgress=false だが配置復元前）に、敵陣へ進んだ味方位置でマーカーが出ていた。`UpdateFormationPreview` のガードに `waveClearCelebrating` を追加し、さらに `DetectFormations` を「自陣（IsDeploymentNode Team1）のユニットのみ」に限定（戦闘で敵陣へ出た味方は陣形対象外＝戦闘開始時のバフ判定も自陣基準で一貫）。コンパイル0。残：実機でエンブレム取得→該当シナジーの2/4効果が発動すること・戦闘後に敵陣マーカーが出ないことの確認。
- 2026-06-07: **ヒーロー深掘り3要素を実装**（Cowork+Unity MCP、ユーザー要望）。①必殺アップグレード：ラン開始時に1回ヒーロー別2択で必殺強化（新規 `HeroUltUpgradeUI`、モーダル）。`GameManager.HeroUltUpgrade` で `UseHeroUltimate` を分岐（Aldin=盾量/延長、Kagachi=与ダメ/攻速、Vesna=威力/スタン）。②開始ボーナス＋オーラ：選択ヒーローでラン開始リソース差別化（Aldin=防御アイテム/Kagachi=+4G/Vesna=無料リロール+1、`UIShop.GrantFreeRerollStack` 新設）＋盤面にいると戦闘開始で味方に小オーラ（被ダメ-8%/与ダメ+8%/攻速×1.08）。③主人公は将：`BaseEntity.Die` に `TryConsumeHeroReviveForUnit` を追加し1戦1回HP40%自動復活、使い切って再死亡で味方弱体（与ダメ-15%/攻速×0.9）。フラグは戦闘開始でリセット。コンパイル0。残：実機体感・数値R3-balance。[DESIGN_R3-hero-depth.md] 起票＋実装。
- 2026-06-07: **秘力の魔法寄り化＋必殺の演出/効果表示**（Cowork+Unity MCP、ユーザー要望）。①秘力系を「マナ促進＋スキル威力」へ：`BaseEntity.ApplyTimedSynergyPowerBonus` 新設（synergyPowerBonus 時限加算→スキル威力/効果時間UP）。強化マスArcane=マナ獲得×1.5＋秘力+35%＋即時マナ+20、Vesnaオーラ=マナ獲得×1.18＋秘力+12%（攻速/与ダメから変更）。強化マスUIの説明も更新。②必殺の効果表示：`GetHeroUltimateDescription`（アップグレード反映）＋`HeroUltButtonUI` ホバーツールチップ。③必殺カットイン：新規 `HeroUltCutInUI`（全画面・戦闘継続・約1.1s。斜めカラーバンド＋ヒーロー立ち絵＋必殺名＋フラッシュ＋カメラシェイク＋専用SE=他ユニット未使用のアナウンサーボイスをヒーロー別に流用 lyonar/songhai/vanar）。ヒーロー別＋アップグレードで色・タグが変化（例 Vesna A=業火橙/B=追雷電青）。コンパイル0。残：実機で演出/SE/色変化・秘力効果の体感確認、数値R3-balance。[DESIGN_R3-hero-depth.md] 追補。
- 2026-06-07: **タイトルのフローを「はじめから/つづきから」に刷新**（Cowork+Unity MCP、ユーザー要望）。タイトルの「PLAY」を廃止し「はじめから(NEW GAME)」「つづきから(CONTINUE)」を追加（図鑑/オプション/終了は維持）。スロット選択画面をモード対応に：`SlotSelectMode{NewGame,Continue}`＋`ShowSlotSelect(mode)`でタイトル文言切替。【はじめから】全スロット選択可、空き=「ここに作成」、既存=「⚠クリックで上書き」→汎用確認オーバーレイ`ShowConfirm`で上書き確認→`BeginNewGame`（DeleteSlot→SetActiveSlot→必ずヒーロー選択→ロビー）。【つづきから】データ有りスロットのみ選択可（空きは淡色・無効「データなし」）→SetActiveSlot→（旧セーブ未選択ならヒーロー選択）→ロビー。セーブ皆無で「つづきから」を押した場合は違和感なく「はじめから」へ誘導。削除確認も汎用`ShowConfirm`へ統合（旧`pendingDeleteSlot`削除）。実機(プレイ)でタイトルに両項目生成・旧PLAY消去・例外0を確認。コンパイル0。残：各遷移の手触り確認。
- 2026-06-07: **ボス陣営 Magmar(f5) の将3体をビルド（縦スライス第1弾）**（Cowork+Unity MCP）。DESIGN_R3-boss-factions の方針（ch4-6=Magmar/7-9=Abyssian/10-12=Vetruvian/13=Arcana、各陣営base/alt/third、縦スライス、仮命名）に着手。UnitAnimationBuilder に `Magmarvaath(f5_general)` `Magmarstarhorn(f5_altgeneral)` `Magmarragnora(f5_3rdgeneral)` を追加（T5/cost5、stat暫定）。`BuildUnitsByNames` で reference/duelyst から plist/png 自動コピー→スプライト/prefab/Animator(idle/move/attack/ability/dead)生成→DB同期（55→58体）。3体とも prefab/icon/アニメ完備をDBで確認。シナジー割当（仮）：Vaath=Beast/Frenzy/Inferno、Starhorn=Storm/Beast/Warrior、Ragnora=Inferno/Beast/Summoner。`ChapterBossRewardUnitIds` の ch4-6 を Magmar将へ更新（ch7-12は対応ボス将ビルド後に差替予定）。コンパイル0。**残（次手）**：章4-6のラウンド構築（BuildChapter4/5/6Rounds＋case＋PlayableChapterCount→6）、固有スキル/紹介文、章ボスSE=magmarボイス。[DESIGN_R3-boss-factions.md] 着手。
- 2026-06-07: **ボス戦前の掛け合いダイアログ（ヒーロー将 vs ボス将）**（Cowork+Unity MCP、ユーザー要望）。新規 `HeroBossDialogueUI`（VN風・全画面）：左にボス・右にヒーローの立ち絵、下部にセリフ枠＋名前プレート、話者を強調、クリックで進行、スキップ可。`DebugFight` で中ボス/章ボスウェーブの開始前に1回だけ割り込み表示し、完了で実戦闘へ（`bossDialogueShownThisWave` フラグ、ウェーブ進行/ジャンプでリセット）。`GetWavePrimaryBossId` で顔役ボスID取得（章ボス=報酬ID/中ボス=候補先頭）。セリフは3行（ボス挑発→ヒーロー返し→ボス締め）で、ボスID別＋ヒーロー別＋汎用フォールバック（Magmar3将・ヒーロー3種に固有文＝仮）。立ち絵：ヒーロー=HeroArt、ボス=EntityDataアイコン（暫定。将来は陣営general立ち絵に差替余地）。コンパイル0。残：実機で掛け合い表示→戦闘開始の流れ・文面調整。
- 2026-06-07: **ラウンド前の敵事前配置＋入場演出（全ラウンド）**（Cowork+Unity MCP、ユーザー要望）。編成フェーズ中に次の戦闘ラウンドの敵を事前生成して見える化。`GameManager.PreviewWaveEnemiesIfNeeded`（Updateから・ウェーブ毎1回・コア戦/イベント行は除外、`enemiesPreviewedThisWave`フラグ。ウェーブ進行/ジャンプでリセット）。`BaseEntity.PlayEntranceAnimation`（新規）＝最終位置から+1マス(+x)離れた地点でα0→1フェードイン＋walkアニメで歩いて着地（約0.6s）。`SpawnWaveEnemy` で生成直後に呼ぶ。`DebugFight` はプレビュー済みなら敵を再生成せずそのまま戦闘開始（入場の二重化防止、ボス掛け合いとも両立）。敵は CanAct=IsRoundInProgress 必須のため編成中は静止＝安全。コンパイル0・ロビー例外0。残：GameSceneでの実機確認（編成中に敵が歩いて出現→FIGHTで戦闘）。
- 2026-06-07: **チャプター4-6（Magmar陣営）を実装＝縦スライス完了**（Cowork+Unity MCP）。汎用ジェネレータ `BuildScaledFactionChapter(chapter, chapterBoss, elites, recruit3, recruit4, escorts)` を新設し、章番号で難度スケールする4ステージ/全33ラウンドを生成（Stage1=やさしい立ち上がり／2-3-4=戦闘＋イベント＋中ボス／4-10=章ボス）。`BuildChapterRounds` に case4-6 を追加：ch4=Magmarvaath / ch5=Magmarstarhorn / ch6=Magmarragnora を章ボスに、同胞3将をエリート敵として各章に登場させ「同じ種族」感を出す。中ボス勧誘候補は既存ボス級（Malyk/Paragon/Grymbeast/Cinderwraith/Draugarlord/Kingsguard/Dissonance）、章ボス護衛は cost5（Gol/Kron/Invader）。`LobbyUI.PlayableChapterCount` 3→6。参照ユニットは全てDB登録済み（Magmar3将＋既存）。コンパイル0。残：実機で ch4-6 を選択・進行（ch3クリアで解放）、ボス戦で Magmar将登場＋掛け合い＋章クリアで解放/強化演出、balance調整。次は Abyssian(f4) の将3体ビルド→章7-9。[DESIGN_R3-boss-factions.md] Phase進行。
- 2026-06-07: **チャプター7-9（Abyssian陣営）を実装＝縦スライス第2弾**（Cowork+Unity MCP）。Abyssian(f4) の将3体を UnitAnimationBuilder に追加（`Abyssallilithe`(f4_general)/`Abyssalcassyva`(f4_altgeneral)/`Abyssalmaehv`(f4_3rdgeneral)、T5/cost5）→ `BuildUnitsByNames` でビルド＆DB同期（58→61体）。仮シナジー（死・影・深淵・幽鬼）。`BuildScaledFactionChapter` を流用し case7-9 を配線：ch7=Abyssallilithe / ch8=Abyssalcassyva / ch9=Abyssalmaehv を章ボスに、同胞3将をエリート敵に。勧誘候補＝既存ボス級（Shadowlord/Malyk/Ilenamk2/Wraith/Cinderwraith/Dissonance/Draugarlord/Grymbeast）、護衛＝cost5（Legion/Invader/Gol）。`ChapterBossRewardUnitIds` ch7-9 を Abyssian将へ更新。`PlayableChapterCount` 6→9。掛け合いダイアログに Abyssian将の固有セリフ追加。コンパイル0／DB確認。残：実機で ch7-9 進行・balance。次は Vetruvian(f3) の将3体→章10-12、最後にArcana章13。[DESIGN_R3-boss-factions.md] Phase進行。
- 2026-06-07: **チャプター10-12（Vetruvian陣営）＋13（Arcana最終章）を実装＝ボス陣営ロードマップ完了**（Cowork+Unity MCP）。Vetruvian(f3) 将3体（`Vetruvianzirix`(f3_general)/`Vetruviansajj`(f3_altgeneral)/`Vetruvianscion`(f3_3rdgeneral)、T5/cost5）をビルド＆DB同期（61→64体）。仮シナジー（機巧・秘術・聖光・王権）。`BuildScaledFactionChapter` で case10-12 配線（ch10=zirix/ch11=sajj/ch12=scion、勧誘候補＝既存ボス級、護衛＝cost5）。最終章 ch13=Arcana を追加：全陣営の頭目（Magmarvaath/Abyssallilithe/Vetruvianzirix）をエリート、各陣営third＋cost5を護衛にした総力戦。`ChapterBossRewardUnitIds` ch10-12 更新、`PlayableChapterCount` 9→13。掛け合いに Vetruvian将＋Arcana の固有セリフ追加。参照ユニット全てDB存在を確認。コンパイル0。**これで章1-13・6陣営（ヒーロー3＝f1/f2/f6、ボス3＝f5/f4/f3）＋Arcana が一通り実装**。残：全章の実機通し・balance調整・各将の固有スキル/紹介文。[DESIGN_R3-boss-factions.md] 完了。
- 2026-06-11: **既存ユニットへ陣営シナジー付与＋Songhai新4体＋シナジーアイコン刷新＋熟練度カーブ＋デバッグ機能**（Cowork、ユーザー要望）。(1)シナジーアイコン: duelyst runes をスライスし全25種を固有アイコン化（`Assets/Resources/SynergyIcons/*.png`）、`SynergyIconLibrary.GetResourcePath` を名前ベース化（新シナジーは同名PNGを置くだけ）。(2)熟練度: 最大Lv12→50、必要XP=8+(Lv-1)*4 の逓増カーブ（Lv50累計5096XP）、ステ倍率+4%→+1%/Lv。ゲージ/デバッグ対応。(3)デバッグ: F8メニューに熟練度XP強制付与＋章クリア扱いを追加。(4)準備画面→ヒーロー変更導線＋熟練度ゲージ。(5)**既存38ユニットへ陣営シナジーを付与**（各陣営7体: Lyonar/Vetruvian/Abyssian/Magmar/Vanar、Songhai既存3）。`Entity Database.asset` を直接編集（synergy1へ陣営をprepend・旧syn3をdrop）。Skindogehaiは除外（カガチ用スキン運用）。(6)**Songhai新4体**（Lanternfox/Onyxjaguar/Keshraifanblade/Firewyrm、f2アトラス）を UnitAnimationBuilder/ResolveDefaultSynergies/Localization に追加。**残（Unity要）**: 全プロジェクト再インポート(ForceUpdate)でMCPブリッジ無応答中。復帰後にSonghai4体をビルド＆DB同期、.asset編集の反映確認、各陣営カウント7体検証、errCS=0確認。
- 2026-06-11: **「1マスに複数体」報告の調査＋重複検出器追加**（Cowork+Unity MCP）。Playモードで一時停止して実データを検査：味方の盤面ユニットは複数フレーム/複数バトルで**全て別ノード**（node index 重複0）＝論理的なマス重複は無し。スクショの重なりは高速再生(×8)中のアニメ補間による見た目の重なり＋召喚トークン(Zyx)の一時表示と判断。`PlacedTeam1Count` が `PlacementLimit` を超える件は召喚トークン(Zyx×2)が盤面枠外で生成されるためで、配置バグではない。念のため `AutoPlayHarness.CheckOverlap` を追加：戦闘中に同一ノードへ味方2体以上を検出したら `AutoPlayDumps/overlap_report.log` に記録（実際に起きた場合の証跡用）。コンパイル0。
- 2026-06-11: **AutoPlay指摘3点の修正（ダイアログ/移動/ウロウロ検出）**（Cowork+Unity MCP、ユーザー要望）。①ボス戦前ダイアログ：`HeroBossDialogueUI.DebugAutoResolve`(skip)を追加し harness の ResolvePopups 最優先で処理→「未処理のまま戦闘開始」を解消（DebugFight二重呼び防止）。②メレー移動：`BaseEntity.GetInRange` を**安定ゴール(moveGoal)＋迂回経路**方式に刷新。毎着地での目的地フリップ（斜め2マスのウロウロ）を解消し、占有マスは GetPath が自動迂回（遠回りしてでも攻撃足場へ）。到達不能（味方が敵を囲み切っている等）の時のみ待機。`PickAttackGoal`(射程内の到達可能な空き足場を最短歩数で選択)追加。③`AutoPlayHarness.CheckWander`：戦闘中に味方/敵の生存数が一定時間(既定10s)変化しない＝手詰まりを検出し `AutoPlayDumps/wander_report.log`＋`_wander/` に記録（ランは継続）。コンパイル0。残：実機で迂回挙動とレポート生成の確認。[DESIGN_autoplay.md]
- 2026-06-11: **オートプレイ・ハーネス v2（賢いボット）＋実バグ発見・修正**（Cowork+Unity MCP、ユーザー要望）。ボットの質を向上：①主人公を盤面投入（無料枠）②XP購入でレベル上げ（盤面上限＝Lv）③利子(所持金/10,上限5)を意識した確保下限つき資金管理④ベンチは星が高い順に選択⑤近接=前列/遠隔=後列のロール配置（GetNodeAtBoardCoordinate/GetBoardColumn）⑥重複購入で星up狙い⑦所有シナジーを伸ばす購入＋限定リロール⑧`GameManager.DebugAutoEquipBenchItems` でアイテムを最良ユニットへ装備。ゲームオーバー時に `AnalyzeGameOver` で敗因推定（早期敗北/盤面未充足/シナジー無し/星up無し/低Lv/アイテム無し）＋詳細スナップショット＋累積学習ログ `AutoPlayDumps/gameover_analysis.log`、連続早期敗北で確保下限を緩める簡易学習。**Playスモークで実バグ検出**：`AttackEffectPlayer.PlayAnimatedSprite` がラウンド終了で破棄された SpriteRenderer を参照し MissingReferenceException（高速周回で多発）→ null ガードで修正。コンパイル0。残：勝率/採用率の統計集計（次フェーズ）。[DESIGN_autoplay.md]
- 2026-06-11: **オートプレイ・ハーネス v1（開発デバッグ専用）**（Cowork+Unity MCP、ユーザー要望）。標準ヒューリスティックのボットでフルランを自動周回し、進行不能（署名が30秒変化しない）と例外/エラーlog（Application.logMessageReceived）を検出して `AutoPlayDumps/<日時>_<理由>/`（report.txt＋log.txt）を出力。新規 `Assets/Scripts/Debug/AutoPlayHarness.cs`（#ifガード/DontDestroyOnLoad/F8メニューから起動・速度・最大章・1/5/20周）。各選択UIに `DebugAutoResolve`（Augment/Item/Boss/HeroUlt）・`BuffTileRewardUI.DebugChooseFirstType`＋`GameManager.DebugResolveBuffTilePlacement`・`ResultPanelUI` の結果参照/`DebugContinue` を追加。購入=UIShop.OnCardClick、配置=GetFreeNode+TryPlaceEntityManually、章開始=PendingStartChapter+GameScene。コンパイル0／Playスモーク：自動周回が回りDumpは未発生（クラッシュ無し）。残：勝率/採用率/章別クリア率の統計集計（次フェーズ）、購入/配置ヒューリスティックの高度化。[DESIGN_autoplay.md]
- 2026-06-11: **【完了】上記の検証（エディタ再起動後）**。Unity復帰後に確認：(1).asset編集の38体分の陣営シナジーは再インポート後も保持。(2)UnitAnimationBuilder.BuildUnitsByNames で Songhai新4体（Lanternfox/Onyxjaguar/Keshraifanblade/Firewyrm）をビルド＆DB同期（71→75体、prefab/icon/synergy=Songhai+テーマ2 を確認）。(3)各陣営の非ヒーロー=7体（ヒーロー3込みで全6陣営=合計10体）。(4)errCS=0。残：実機でショップ/シナジーアイコン表示・balance。Skindogehaiはスキン運用のため対象外。
- 2026-06-13: **ストーリー実装 第1弾＝ボス戦前台本テーブル＋幕間/エンディングVN枠＋カガチ犬化スキン**（Cowork+Unity MCP、ユーザー「実装に入りましょう」）。(A)ボス戦前ダイアログ：`HeroBossDialogueUI` に boss×hero の確定3行台本テーブル（13章ボス×基本3主人公=39組、`docs/Story/STORY_DIALOGUE_DRAFTS.md`由来）を追加。無い組合せ（中ボス/残り主人公）は従来の汎用文へフォールバック。Arcanaの覇王口調プレースホルダを「確定設定v1」のいたわり口調へ差し替え。(B)幕間VN枠：新規 `InterludeUI`（ト書き/ナレーション/セリフのN行スクリプト、立ち絵1枚切替、クリック送り＋スキップ、`EnsureExists`/DOTween/JA-EN）＋台本データ `InterludeScript`（INT_01合流/INT_08犬化/INT_12観測者の正体/INT_13最終説得/ENDING共通＋主人公別の締め・エピローグ、`STORY_INTERLUDES.md`/`STORY_ENDINGS.md`由来）。表示名→立ち絵IDの対応表で HeroArt/BossArt を流用、「主人公」は選択ヒーローに差替。(C)トリガー（`GameManager`、章モードのみ）：INT_01=ch4-6初回の最初の戦闘前／INT_08=ch8章ボス前・カガチ限定（通常3行を抑止）／INT_12=ch12クリア時・結果表示前／INT_13→ENDING=ch13クリア時・結果表示前に連鎖。各1回限り（`SaveManager` の汎用 storyFlags で既読管理＝`SaveData.storyFlags`＋`Get/SetStoryFlag`）。(D)カガチ犬化スキン：ch8をカガチで踏破→`skin_kagachi_unlocked`解放。ヒーロー選択カード（`LobbyUI`）に「出撃姿: 人/犬」トグル（`skin_kagachi_on`）。ON時 `GrantHeroUnit` が `HeroKagachi`→`Skindogehai`（既存の専用prefab/controller/skill/balanceを流用）へ差替えて出撃。AutoPlay harness に `InterludeUI.DebugAutoResolve` を追加（バッチ周回でも幕間で止まらない）。`GetEntityIconById` を public 化。コンパイル0（realCS=0）／データ層スモーク（INT_01=22beats・立ち絵ID解決OK）。残：実機で各幕間の表示・立ち絵欠落チェック、犬化スキンの実戦表示、残り6主人公×ボスの3行台本（GPT執筆・計117組）、ハブの回想ビューア。[docs/Story/*]
- 2026-06-13: **ダイアログ実機フィードバック対応（立ち絵統一・拡大・左右反転・可変行数・中ボス個別化）**（Cowork+Unity MCP、ユーザー指摘）。(1)立ち絵素材を `Assets/Images/Dialog`→`Assets/Resources/UI/Dialog` に一本化（@2x除去で軽量化、Hero‗*→hero_* にリネーム）。新規 `DialogArt.Portrait(id)` で ID→大判イラスト(general_f<陣営>/alt/third・2560x1850、hero_aldin/kagachi、general_boss_1、general_unknown) を一元解決し、無ければユニットアイコンへフォールバック。`HeroBossDialogueUI`/`InterludeUI`/`HeroUltCutInUI`/`LobbyUI`(計8箇所) の `Resources.Load("UI/HeroArt|BossArt")` を全て DialogArt 経由に置換。**旧 Resources/UI/HeroArt・BossArt を削除**（重複解消）。(2)両ダイアログUIの立ち絵を画面いっぱいに拡大（620→1080x1180）。(3)**敵=右/味方(主人公)=左** に統一（ゲーム中の自軍盤面が左のため）。(4)ボス台本を3行固定→**可変行数**（偶数=ボス/奇数=主人公の交互）。(5)中ボスの「全員同じセリフ」を解消：汎用フォールバックをボスID安定ハッシュで6種から振り分け＋主人公9人分の返しを追加。(6)長尺本文はGPT執筆へ（`docs/STORY_brief_dialogue_v2.md` に書式・長さ・bossId/heroId一覧を用意）。コンパイル0（realCS=0）／DialogArt解決を実機確認（general_f*=2048x1480）。**git index 破損**を検出し index 再生成で復旧。残：実機で立ち絵の見栄え最終確認、中ボスIDの章別抽出→GPTへ、長尺台本の流し込み。
- 2026-06-14: **章を13→20章へ拡張＋中立ボス9体を新規登録**（Cowork+Unity MCP、Codex引き継ぎ docs/CLAUDE_HANDOFF_CHAPTER20_BOSSES.md）。新ボス: 1=Caliber/2=neutral_rook/3=neutral_sister/4-6=Magmar/7-9=Abyssian/10-12=Vetruvian/13-18=Mechaz0r連章(wing/sword/super/helm/chassis/cannon)/19=neutral_hydrax/20=Arcana(最終章へ移動)。(1)中立ボス9体(rook/sister/hydrax/mechaz0r*6)を `UnitAnimationBuilder` に spec追加→`BuildUnitsByNames` で reference/duelyst の neutral_* アトラスから prefab/animation/Entity DB をビルド(75→84体)。Localizationに日本語名追加。(2)`ChapterBossRewardUnitIds` を1-20欠番なしへ更新(`GetAllChapterBossRewardUnitIds()`=20体)。(3)`BuildChapterRounds` に case13-20追加(13-19=中立プール `MechaElites/Recruit/Escorts` で `BuildScaledFactionChapter`、20=Arcana総力戦)。旧13章Arcanaは廃止。(4)章2/3の4-10ボスを Solfist/Dissonance→neutral_rook/neutral_sister に差替(ソルフィスト/ディソナンスは通常ユニットとして存続)。(5)`LobbyUI.PlayableChapterCount` 13→20(gate_000=1章…gate_019=20章、解放はN-1クリアで既存ロジック)。中立ボスは章ボス報酬として `AddBossAlly` で恒久解放されるため ReservedGeneralには入れない(Caliber等と同パターン、cost4/5でショップ既定ロック)。コンパイル0(realCS=0)／検証: 全20章ボスIDが Entity DB に存在しprefab非null(20/20)。残: 実機で20章カード表示・各章ボスspawn・balance、ボス戦前台本の20章版(Codex執筆)、中立ボスのダイアログicon割当(AddUnit/dialogue‗icon)。

- 2026-06-17: **中ボス会話の非ブロッキング字幕化＋章ボス直前を進路選択(midboss-nodes)化**（Cowork+Unity MCP、ユーザー要望「3連中ボスがテンポを悪くする」→Slay the Spire風ノード選択を採用）。(A)中ボスのかませを `ChatterSubtitleUI`(非ブロッキング)で流し戦闘を止めない。章ボスのみフル会話(`HeroBossDialogueUI`)維持。(B)進路選択：章ボス直前の中ボス3連(4-7/4-8/4-9)を廃し、3ノード提示→**逐次2選択→残り1破棄**(実戦3→2でテンポ改善)。新規 `NodeSelectionUI`(カード式/EnsureExists/JA-EN/DOTween)。`WaveDefinition.IsNodeChoice` 追加、`DebugFight` にノード選択フック、`ApplyMidBossRewardSchedule` はノード枠を除外、選択時に `ApplyNodeToWave` で枠の敵編成/候補/報酬を確定。アーキタイプ=🔴精鋭(良報酬・歯ごたえ)/🟡標準(仲間化)/🟢補給(易・大量コイン)。全章対応：章1/2/3(手組)＋共通 `BuildScaledFactionChapter`(章4-20、精鋭は陣営エリートE0を主役・cost4★2上限順守)。コンパイル0(realCS=0)。残：実機でUI見栄え/逐次フロー/章ボスへの遷移確認、AutoPlay用の自動選択フォールバック、精鋭と標準の難度差(★2上限内)のbalance調整。[docs/DESIGN_midboss-nodes.md]

- 2026-06-17: **強撃プレースホルダ→固有スキル化（ボス＋勧誘可能 計28体）完了**（Cowork+Unity MCP、ユーザー要望）。既定の強撃に落ちていた章ボス＋勧誘可能ユニットへ、個性に沿った非重複の固有スキルを付与。各体 `BaseEntity.TryExecuteDedicatedSkill`分岐＋`Execute*`本体＋`UnitStatusPanelUI`のスキル名＋実数値説明(JA/EN)。バッチ実装：①早期章ボス(caliber終幕の薙ぎ/neutral_rook停滞の城門/neutral_sister鎮魂の哀歌)②中立recruit5(beastmaster群獣の咆哮/gnasher顎砕き/rawr威圧の咆哮/rok岩石投擲/zukong如意千変)③Songhai4(lanternfox灯火の導/onyxjaguar影駆け/keshraifanblade扇刃乱舞/firewyrm業火のブレス)④後半章ボス16(Magmar3/Abyssian3/Vetruvian3/mechaz0r6/hydrax)。既存プリミティブ(DealLegendarySkillDamage/ApplyShield/HealSelf/ApplyAttackSpeedSlow/ApplyStun/ApplyVulnerable/ApplyWeaken/ApplyTimedSynergy*/ApplyAttackSpeedBoostFromSynergy)で構成、数値ヘルパ(GetAreaDamage/GetPowerStrikeDamage/GetShieldAmount/GetAllyHealAmount/GetSkillDuration)で説明と実値を一致。コンパイル0(realCS=0)。雑魚(neutral_z0r等)は強撃のまま。残：実機でスキル発動の見栄え/バランス、★3スケールの調整(R3-balance)。[docs/DESIGN_skill-fill.md]

- 2026-06-17: **ボス育成スキルを再設計（戦闘中マス生成を撤回→踏む不要の複雑効果へ）＋全章ボス展開**（Cowork+Unity MCP、ユーザーFB）。戦闘中の強化マス生成は「戦闘後に残る/敵陣に出る/踏むか不確実」で失敗→撤回。代わりに(1)`GameManager.EmpowerAlliesAround`＝caster周囲の味方を直接時限強化（マス/マーカー無し・戦闘終了で自然消滅）(2)`BaseEntity.DisplaceEnemiesAround(pull/push,cells)`＋`ForceRelocateTo(Node)`＝敵の盤面位置を引き寄せ/ノックバック（Node占有/盤外を安全処理、見つからなければ移動しない）。育成Lv7+の複雑効果を `BaseEntity.ApplyBossComplexTier` に集約（TryExecuteDedicatedSkill成功後にフック）。全章ボス19体へ割当：caliber=ノックバック＋守護強化/rook=引き寄せ＋(最大)根固め/sister=全体弱体＋治癒/magmarvaath=引き寄せ/starhorn=大ノックバック/ragnora=味方堅守/lilithe=全敵AoE吸命/cassyva=全体スタン/maehv=引き寄せ/zirix=ノックバック/sajj=味方攻撃強化/scion=対象拘束/mecha wing/super=味方攻撃強化,helm/chassis=押し出し,sword=対象スタン,cannon=着弾スタン/hydrax=ノックバック/arcana=味方秘力強化。最大Lv(11)で各効果が強化。ダメージ自体は前コミットの共有計算注入で育成Lvスケール済み。realCS=0。残：実機で敵移動の破綻有無・バランス（R3-balance）、スキル説明文への育成段階の明記。[docs/DESIGN_boss-skill-progression.md]

- 2026-06-25: **Steam リリース作成基準を策定＋現状リスク監査**（Cowork、ユーザー要望「審査に落ちない基準づくり」）。新規 `docs/STEAM_READINESS_STANDARDS.md`。Steamworks 公式の Review Process を一次出典に、審査が実際に見る4点（起動/ストア整合/権利・年齢/Steam Wallet）を整理。アセット法務を調査し **OpenDuelyst(github.com/open-duelyst/duelyst) はコード・アセット・音楽・SFX 全て CC0-1.0**＝商用可/帰属不要で著作権リジェクトのリスクは低いと確認（出典: LICENSE/COPYING）。残る現実リスクを優先度付けで監査: ①ロビーの「Coming Soon/準備中」デッドエンド(🔴 EA で出すか launch ビルドで非表示か要決定) ②productName `AutoChess Boss Rush (Alpha)`(🟡 "Auto Chess"商標連想+"(Alpha)"、正式タイトル確定後に改称) ③陣営名 Lyonar 等が Duelyst 由来でプレイヤー表示(🟡 商標観点で独自名が安全、ローカライズ表だけ差替で対応可) ④出所不明 SFX(notification/pointdrop/select/Sell)(🟡 要確認・必要なら差替) ⑤未使用サンプルフォント同梱(🟢) ⑥クレジット画面なし(🟢 推奨) ⑦Steamworks 未統合(🟢)。クリア済: 設定(解像度/全画面/言語/音量/品質)・終了ボタン・セーブ原子書込・JA/EN・Win x64 ビルド実績。INDEX 同期済み。残: ①②③の方針決定後に実装着手。

- 2026-06-25: **「準備中」要素を実装し製品版化（コレクション＋ショップ選抜ハブ）**（Cowork+Unity MCP、ユーザー方針「準備中は実装して製品版で出す」、[docs/DESIGN_R4-collection-hub.md]）。ロビーの2つの Coming Soon（①プレイモード「ユニット編成」カード ②出撃準備「ショップに出すユニットを選ぶ」）を実機能化し、コレクション＋ショップ選抜ハブ `CollectionHubUI`（新規）に統合。(1)`SaveData.shopDisabledUnitIds`（List<string>・storyFlags方式）追加＋`SaveManager.IsShopUnitEnabled/SetShopUnitEnabled`。(2)`GameManager.IsShopRosterCandidate`（静的・母集合判定＝prefab有/cost1-5/ヒーロー将系雑魚召喚を除外）追加、`IsEntityUnlockedForShop` に選抜チェック1行追加（UIShop改変不要で全抽選が追従）。(3)`CollectionHubUI`：ScreenSpaceOverlay Canvas・コスト別スクロール・各ユニットON/OFFトグル・**コスト毎最低2体保証（割る操作は拒否＋赤フラッシュ）**・JA/EN。(4)`LobbyUI`：UNIT FORMATIONカードと出撃準備ボタンをハブ起動に配線、章ゲートの古いコメント修正。母集合=85体（cost別12/11/20/20/22）で枝枯れ無し。コンパイル realCS=0（SaveManager往復はPlayモード専用のためEdit検証不可→コード/規約で担保）。STEAM_READINESS_STANDARDS のリスク①②を「対処済み」に更新。残：実機でハブUI見栄え・選抜のショップ反映・セーブ往復の最終確認。

- 2026-06-25: **編成ハブの選抜制限を強化（★3量産防止＋チャプター限定仲間を非表示）**（Cowork+Unity MCP、ユーザーFB）。(1)ショップ選抜の最低数を一律2→**コスト別 `MinEnabledByCost`={1:9,2:8,3:7,4:6,5:5}** に強化（実効値=min(目標,表示総数)）。連れていくユニットを絞りすぎると同じユニットを引き続けて★3が簡単になる問題への対処。(2)新規 `GameManager.IsHubManageableUnit`（静的）でハブの表示母集合を「恒久プールのみ」に限定＝cost1-2／cost3スターター／`HasBossAlly`（恒久仲間化ボス）。**チャプター内でのみ仲間化される一時解放ユニット（中ボス勧誘候補cost3-5）はハブに表示しない**。`CollectionHubUI` の母集合を `IsShopRosterCandidate`→`IsHubManageableUnit` に変更、最低数判定を配列化。realCS=0。[docs/DESIGN_R4-collection-hub.md]

- 2026-06-25: **陣営シナジーを「主人公ゲート＋同陣営増幅」に改修**（Cowork+Unity MCP、ユーザーFB「1体で簡単に起動できてしまう」、[docs/DESIGN_R3-faction-hero-gate.md]）。(1)`GameManager.ActiveHeroHasSynergy(type)` 追加（現主人公のEntityEdataシナジーに該当陣営が含まれるか）。(2)`CountSynergiesForTeam` で Team1の陣営シナジーは effective が1〜2 かつ主人公が同陣営でないなら 0 扱い→ `ResolveTier`（＝主人公非同陣営は1〜2体で不発、3体から従来通り発動。敵Team2はゲート無し）。(3)効果適用ブロックに `FactionHeroScale`（主人公同陣営なら `FactionHeroBonusScale`=1.5倍、Team1のみ）を導入し全陣営の段階ボーナス（被ダメ減/攻速/与ダメ/スキル威力/シールド/マナ）を×hs。10段階の決定打は対象外。(4)説明更新：各陣営1段階目に「(主人公が同陣営のみ)」、陣営要約末尾に「1体目は主人公同陣営のみ／3+不問／同陣営で+50%」注記(JA/EN)。realCS=0、6ヒーローの陣営割当（Aldin/Ziran=Lyonar, Kagachi/Reva=Songhai, Vesna/Kara=Vanar）を確認。残：実機でbalance（1.5倍/ゲートの体感）。

- 2026-06-25: **主人公ごとの専用フォーメーション（6マス）を追加**（Cowork+Unity MCP、ユーザー要望、[docs/DESIGN_R3-hero-formation.md]）。既存の汎用4陣形に加え、現主人公固有の6マス陣形を実装。`GameManager`に `GetActiveHeroFormation`（6基本ヒーロー固有＋ボス将は陣営フォールバック）/`DetectHeroFormation`（自陣占有マスを3×3相対形で並進不変検出）/`ApplyHeroFormationEffect`（既存時限バフAPIで付与, dur=60s）/`HeroFormEffectText`/`HeroFormCells3x3` を追加。形6種＝聖盾の壁(縦2×3)/日輪の陣(横3×2)/双牙(双柱)/疾風の階(階段)/氷の城門(上下2列)/凍嶺の鉤(L)、効果6種=Bulwark/Sun/Fangs/Gale/Gate/Hook。`ApplyFormationBonuses`末尾で検出→6体へ専用バフ＋エフェクト。`UpdateFormationPreview`で成立6マスを金色ライブ表示。`FormationHintUI`に動的な主人公行(5行目)＋`SetHeroFormation`を追加（形/名/効果/成立ハイライト、パネル拡大）。realCS=0。残：実機でbalance（6マス要求と効果の強さ）、形のUX。

- 2026-06-25: **主人公専用フォーメーションをスプレッド型に変更**（Cowork+Unity MCP、ユーザーFB「もっと配置を離せる陣形に」）。各形を横方向に1マス空けたスプレッド型へ（最大5列×3行に収まる）。双柱(間1)/横スプレッド/遠柱/階段スプレッド/格子/鉤スプレッド。密着不要で置きやすい。ガイドの主人公行を3×3→5×3表示に拡張（HeroFormCells5x3, FormationHintUIのheroCells[15]）。realCS=0。

- 2026-06-25: **仲間化できる中ボス19体のシナジーを個性化**（Cowork+Unity MCP、ユーザー要望「それぞれ個性を」）。全員が汎用デフォルト(Warrior/Guardian or Ranger/Arcanist)で没個性だった問題を解消。`ResolveDefaultSynergies` の switch に19ケース追加＋Entity Database.asset の当該19体の synergy1/2/3 を None に戻して switch を適用。割当（各ユニーク）：中立コア5=beastmaster(Beast/Summoner/Guardian)/gnasher(Beast/Frenzy/Shadow)/rawr(Beast/Warrior/Royal)/rok(Guardian/Storm/Machine)/zukong(Warrior/Storm/Frenzy)、リカラー変種5=bm_crimson(Beast/Inferno/Frenzy)/gnasher_ice(Beast/Frost/Shadow)/rok_steelblue(Guardian/Frost/Machine)/rok_gold(Guardian/Divine/Royal)/rok_mossgreen(Guardian/Beast/Alchemy)、陣営勧誘候補9=Silitharelder/Veteransilithar/Makantorwarbeast(Magmar系)/Gloomchaser/Abyssalcrawler(Abyssian系)/Rae/Starfirescarab/Pax(Vetruvian系)/Pyromancer(Inferno/Arcanist/Storm)。陣営勧誘候補は陣営シナジーを持ち、同陣営を集めると陣営シナジーに寄与（faction-hero-gateと連動）。realCS=0、19/19が新シナジーを返すことを確認。[docs/DESIGN_R3-midboss-synergy.md]

- 2026-06-25: **メレーが敵の斜めで詰まって周回する不具合を修正（射程判定をグリッド距離へ）**（Cowork+Unity MCP、ユーザー報告「斜め2マスをウロウロ」）。原因：盤面はマス間隔1.0・移動グラフは縦横斜め1マスを隣接(max(|dx|,|dy|)<=1)とするのに、射程判定だけユークリッド距離(≤range+0.05)で、range1のメレーは斜め隣接(≈1.41)が射程外→斜めに居着くと攻撃できず直交の攻撃マスを探して往復していた。`BaseEntity.IsTargetInRange` と `GridManager.GetNodesInRange`、`GetInRange`のgoalValid判定をチェビシェフ距離 max(|dx|,|dy|) に統一。これで斜め隣接でも攻撃可能になり攻撃足場にも斜めマスが含まれ、周回が解消。realCS=0。残：実機で各ユニットの接敵挙動確認、（必要なら）previousNodeを使った往復抑制の追加。

- 2026-06-25: **チェスト報酬ラウンド part2/2（殴って開ける宝箱部屋）統合**（Cowork+Unity MCP、[docs/DESIGN_R3-chest-room.md]）。`MidBossRewardKind.ItemChoice3` の回を `WaveDefinition.IsChestRoom` 化し、`PreviewWaveEnemiesIfNeeded` で敵/シネマティックの代わりに `StartChestRoom` を呼ぶ。コインチェスト（章で3〜6個・前〜中列）＋アイテムチェスト1個（最奥）を team2 にサンドバッグ生成。コイン箱=被弾でHP6分割ごとに1コイン(揺れ+SE)＋HP0開封で+5＝計10。アイテム箱=HP0で `ShowItemChoice3Reward()`(3択)。`GetRoundDamageMultiplier` はチェスト部屋中ずっと×1.0、`HandleChestRoomTimer` が30秒で残り宝箱を未開封のまま撤去してラウンド終了（獲得済みのみ持ち越し）。事後の二重報酬を避けるためクリア時のItemChoice3分配を抑制し `EndChestRoom`。HP=盤面DPS推定(配置数×章プロキシ, ChestClearTargetSeconds=22, 0.6/0.4配分)。チェスト素材は `AttackEffectSpriteBuilder` に追加して fx へ27フレームスライス、閉じた箱の idle 先頭フレームを SpriteRenderer に差し替え（item=通常/coin=festive）。realCS=0。残：HUDの残り秒数表示、宝箱idleアニメ、実機balance。

- 2026-06-25: **チェスト報酬ラウンド 仕上げ（HUDタイマー＋宝箱idleアニメ＋Animator無効化）**（Cowork+Unity MCP）。(1)流用プレハブのAnimatorがスプライトを上書きして宝箱が表示されない問題を修正：`ConfigureAsChest` で `animator.enabled=false`。(2)提供された宝箱idleフレームをループ再生（`BaseEntity.SetChestIdleFrames`＋`ChestIdleRoutine`、~11fps）。(3)新規 `ChestRoomHudUI`：画面上部に「宝箱を開けろ！ 残り NN 秒」を表示（残り5秒以下は赤）。`GameManager.IsChestRoomActive`/`ChestRoomSecondsLeft` を公開、`StartChestRoom` でEnsureExists。realCS=0。残：実機でbalance（HP係数・コイン数・配置）と見栄え確認。

- 2026-06-25: **チェスト報酬ラウンドの対象修正＋見た目バグ修正**（Cowork+Unity MCP、ユーザーFB）。(1)当初 `ItemChoice3`(中ボス報酬)をチェスト化していたが**ボス/戦闘ラウンドに影響して不適切**→撤回（`SetMidBossReward`からIsChestRoom削除、事後分配の抑制も撤回）。対象を**アイテムイベント回 `BonusItem`（2-8/3-7 等・全章8箇所）**のみに変更：新規 `StagedChestRoom`（敵なしの通常戦闘ウェーブ・`IsChestRoom`）で置換し、`PreviewWaveEnemiesIfNeeded` の既存分岐で `StartChestRoom`、クリアで `EndChestRoom`。(2)戦闘開始で宝箱がBorealjuggernautに戻る不具合を修正：`ConfigureAnimatorForRuntime` が宝箱はAnimatorを無効のまま維持。(3)アニメ：HP0まで「閉じてキラめく」idleフレームをループ、HP0で「開く」openフレームを1回再生（`SetChestFrames` が名前で idle/open を分離）。realCS=0。残：実機で見え方/30秒/報酬/balance確認。

- 2026-06-25: **章プロローグ/オープニングVNの二重・チラ見え不具合を修正**（Cowork+Unity MCP、ユーザー報告）。(1)プロローグ終了時、黒フェードアウトの前に自分の一枚絵/字幕を隠すように変更（隠さないと黒明けで「最初のプロローグ画面」が一瞬再表示されていた）。`ChapterPrologueUI.Run`。(2)オープニングVN(CHOPEN)が二重再生する不具合：章開始処理（プロローグ→VN→ショップ, GameManager 458）で再生済みなのに、`TryPlayPrefightStory`(6428)が1-1のFIGHT時にも再度CHOPENを出していた→この重複経路のCHOPENブロックを削除（INT_01/INT_08は維持）。realCS=0。

- 2026-06-25: **体力バーの位置をユニット毎のバラつき→固定に修正**（Cowork+Unity MCP、ユーザー報告「縦に並ぶとバーが重なって見づらい」）。原因：`Healthbar.Update` がバーY位置を `targetRenderer.bounds.max.y`（スプライト上端）に追従させていたため、ユニット毎のスプライト高さ(0.40〜0.60)＋プレハブscaleY(1.02〜1.24)の差でバー高さがバラつき、縦配置で重なっていた。修正：Y位置を「アンカー(セル位置)＋固定オフセット `barFixedRise`(=0.7)」に変更。通常サイズ(〜約1.25倍)は `Mathf.Max(1, scaleY-0.25)` で全て一律固定、大型ボス/★3のみスケール連動で頭上に。横は `bounds.center.x` で見た目中心へ。realCS=0、調整可（barFixedRise）。

- 2026-06-25: **体力バーの横並び重なりでフレームがめり込む問題を修正**（ユーザー報告：横並びにするとバーが重なりフレームが他バーにめり込む）。原因：`Healthbar.UpdateSortingOrder` の `sortingBaseOrder` が `CalculateSortingOrder`（横重み列あたり+3）依存で、1本のバーの内部パーツ幅（フィル+2〜フレーム+8〜アイコン+10、最大+10）より列間隔が狭いため、隣の列のバーと描画帯が混ざりフレームがめり込んでいた。修正：`sortingBaseOrder += Mathf.RoundToInt(target.position.x * 20f)` を追加し、列ごとにバー1本分(+10)より広い間隔を与えて各バーを独立した描画帯に分離（右の列が前面）。行間(yOrder=800)には未到達で縦の重なりは不変。realCS=0。

- 2026-06-25: **章ごとの動的バトル背景を実装**（R4-chapter-background、ユーザー要望：章毎に背景を変え、背景＋前景＋パーティクルで世界観を出す動的背景／戦闘＋ロビー両方／控えめ）。`Assets/Scripts/ChapterBackground.cs` 新規：12テーマ（battlemap0-7＋abyssian/redrock/shimzar/vanar）を20章にストーリー/陣営で割当（ch8=abyssian固定）、背景/中景/前景の視差レイヤー＋テーマ別天候パーティクル（雪/花びら/残り火/灰/塵/青塵/雨/雲）をGameSceneにランタイム構築。全レイヤーは盤面より後ろ（負sortingOrder）で視認性を確保。@2x優先読込。GameManager 章初期化後に `ChapterBackground.EnsureExists().ApplyChapter(currentChapter)`、旧静的 `battlemap1_middleground` はboundsを残してSpriteRendererのみ無効化。検証：realCS=0、エディタ上で全20章を一時カメラで構築テスト→例外0・テーマ別レイヤー数/パーティクル一致。ロビー連動は次コミット。Refs: docs/DESIGN_R4-chapter-background.md

- 2026-06-25: **ロビー背景も章テーマ連動に**（R4-chapter-background ロビー編）。`LobbyUI.EnsureBackground` を従来のランダム `Play/LobbyBg` から、章テーマの合成背景（背景＋中景＋前景のUI Image積層＋控えめな左右ドリフト）に変更。テーマは戦闘と同じ `ChapterBackground.Get*SpriteForChapter` を流用。基準章は `chapterPrepChapter`（出撃準備で章を選ぶと `ShowChapterPrep` から背景を再構築）。Overlay UIのためParticleSystemは使わずUIのみで上品に。realCS=0。Refs: docs/DESIGN_R4-chapter-background.md §5

- 2026-07-02: **⑤フォアグラウンド左右開き演出を実装**（R4-chapter-background、ユーザー要望：章開始時にフォアグラウンドが左右に避けて盤面が見える）。`ChapterBackground` の foreground を「前景カーテン」化：盤面/ユニットより手前(sortingOrder=13000, z=-2)に配置し、章開始時は中央(閉=盤面を覆う)→左右へ `RevealCurtains` コルーチンでスライド(開)、戦闘中は開いたまま維持。2枚組(_001=左/_002=右)は左右へ、単一fgは上へ退避。開き幅は盤面(±4.05)外まで(halfW*1.6)でユニット視認性を確保。実時間(unscaled)進行。検証：realCS=0、ch2で前景2枚生成・例外0。※ロビー背景はユーザー要望で従来ランダムに復帰済み(実FSで確認)。この件のLobbyUI.cs切断事故(bashマウント128KB)は実FSで復旧済み・要ホスト側commit。

- 2026-07-03: **③④章ごとの盤面形状（角丸・保守版）を実装**（R4-chapter-background、ユーザー要望：丸型/非矩形背景に合う盤面形状＝プレイ可能セルを変更）。`ChapterBackground.GetBoardCornerCut(chapter)` が丸型プラットフォームの陣営マップ(abyssian/redrock/shimzar/vanar)で角削り=1、矩形枠のbattlemap系は0を返す。`GridManager.Awake` でグラフ構築前に `ApplyChapterBoardShape` を実行し、四隅タイルを非活性化(角丸)→グラフ/配置/パス/スポーンから自然に除外。敵スポーンは既存の `SpawnWaveEnemy`→`FindAlternateEnemySpawnNode` 同列フォールバックで吸収（角丸は1列1マスしか削らずソフトロック無し）。コア戦/矩形章はフル10×8のまま。検証：realCS=0、GetBoardCornerCut章別値OK、cut=1で四隅4マスのみ非活性を確認。値を上げれば八角形/菱形化も可能（GridManagerが cornerDist<cut を解釈）。ベンチは角丸では未調整（画面端で不変）。②プラットフォーム精密フィットは別途。

- 2026-07-03: **フォアグラウンドが盤面を隠す不具合を修正**（ユーザー報告：ch3/ch4で前景が盤面を覆う）。原因：開いた後も前景が前面(order13000)に留まり、開き幅も足りず盤面端を覆っていた。修正：`RevealCurtains` 開き切り後に前景を盤面より後ろ(ForegroundRestOrder=-150)へ回す `SetForegroundBehind`＋開き幅拡大(2枚組halfW*2.1/単一halfH*2.2)。戦闘中は全章で盤面/ユニットを絶対に隠さない。realCS=0。

- 2026-07-03: **②プラットフォームを盤面にフィット（足場はみ出し解消・第一版）**（ユーザー報告：盤面が背景中央フィールドより広くはみ出す）。`ChapterBackground.AddSpriteLayer` に platform モードを追加し、middleground＋中景層を **カメラ中心ではなく盤面中心(BoardCenterY=-0.04)** に合わせ、**PlatformFitScale=1.3** で拡大してフィールドが盤面(幅8.1)を内側に包むように。背景(sky)はカメラ中心のまま。定数(BoardCenterY/PlatformFitScale)で調整可。realCS=0。※盤面の傾き(C)は、タイル当たり判定がz=0平面前提のため、レイキャスト方式への作替が必要（次段）。※bash側gitがホスト再パック後にオブジェクト書込不可のため、A(前景)＋B(本件)はワーキングツリーに反映済み・コミットはホスト側で実施。

- 2026-07-03: **R5ショップ脱AI着手：通貨アイコンをDuelystマナジェムに差し替え**（DESIGN_R5-shop-duelyst）。出所不明だった `Coin.png` を、CC0のDuelyst素材 `reference/duelyst/app/resources/shop/shop_premium_pack_small.png`（teal crystal 224x224）へ。**GUID保持でファイル名だけ `Coin.png`→`ManaGem.png` にリネーム＋中身差し替え**したため、Slot/T1_*プレハブ・GameScene の参照を一切改変せず（大きいGameScene.unityも無傷で）全表示がマナジェムに切替。Unity再インポートでguid→ManaGem.png/Sprite解決を確認。文言(コイン→マナ)とカードのマナコストジェムはP3/P4で。カードの顔=ユニットスプライト(独立アニメ)＝P1プレビュー基盤が次。※bash側gitはホスト再パック後にオブジェクト書込不可のため、変更はワーキングツリー反映済み・コミットはホスト側で。

- 2026-07-03: **R5-P1/P2 ショップのカードの顔をユニットのドット絵へ（脱AIイラスト）**。RenderTexture案は不採用、図鑑で実績のあるミラー方式を流用：`UICard.Setup` が盤外(-100000)にユニットprefabを生成→`CollectionBossAnimator`で SpriteRenderer を `icon` に写す。各カードが自前プレビューを持つので**5枠すべて自動対応・盤面と別Animatorで非連動**。GameSceneでのStart購読副作用は生成後にMonoBehaviour全無効化で回避（Animatorは別なのでidle継続）、Collider無効化、OnDisable/OnDestroy/Setupで実体破棄（リーク防止）。AIイラスト(EntityData.icon)不使用に。realCS=0。パターンは既存動作コード流用＝確度高、見た目はビルドで要確認。残:P3枠描画/P4通貨文言。

- 2026-07-03: **R5-P3/P4 ショップ脱AI仕上げ**。P3=カード枠のAI画像(`UI/Frame`/`EntityData.frame`)撤去→`UICard.ApplyProceduralFrame`でコスト帯色分けの角丸フレームをプログラム描画(1灰/2緑/3青/4紫/5橙、内蔵UISprite 9スライス)。P4=通貨文言をマナへ（AugmentCatalog 日本語「ゴールド/コイン」→「マナ」9行、英語単独`gold`→`mana`10箇所、ID`gold_*`×25/enum`AugmentTier.Gold`×25は語境界で無傷確認）。カードのコストアイコンは既にManaGem GUIDでマナジェム化済。これでショップは顔=ドット絵/枠=プログラム/通貨=マナジェムで脱AI完了。残(任意):英語オーグメント名の通貨語・P5図鑑。realCS=0。※コミットはホスト側で（bash git不調）。

- 2026-07-03: **脱AIをショップ外の3画面へ横展開**。共通部品 `UnitCardVisual`(枠色/角丸プログラム枠)＋`UnitCardPreview`(盤外にユニット実体を生成→SpriteRendererをImageにミラー、寿命で破棄・盤面と非連動)を新規追加。`BossRewardSelectionUI`/`ChapterRosterUI`/`CollectionHubUI` の AIキャラ絵(EntityData.icon)→ユニットのドット絵、AI枠(EntityData.frame)→プログラム枠 に一括置換。realCS=0。残(低優先):図鑑詳細のアイコン表示トグル/プロローグ一枚絵/ショップ盤UI/英語オーグメント名/未使用AIアイコン88枚+枠5枚の削除。※コミットはホスト側で。

- 2026-07-03: **R5-P6a ショップ脱ShopBoard＋盤面配慮＋ロックトグル**（DESIGN_R5 §6.5）。①GameScene の ShopBoard Image の sprite を `ShopBoard.png`(AI)→`bottom_bar_background.png`(Duelyst CC0) に差替（scene内の当該guid参照は1箇所のみ＝完全に未使用化）。②同 Image の `RaycastTarget:1→0`＝下地が盤面下段のクリック/ドラッグを遮らない＋上端透明グラデで盤面が背景越しに見える＝**盤面への配慮**。`UIShop.EnsureShopChrome()`が実行時にもsprite/raycastを保証。売却判定は自身Rect(`IsPointerOverShop`)なのでraycast無効化の影響なし。③**新機能ショップロック**：`shopLocked`＋`EnsureLockToggle()`でRerollButton直上に「固定/固定中」トグル(button_confirmスキン,JA/EN)。固定中はウェーブ間の自動リロール(`RequestFreeRerollOrPending`/`ConsumePendingFreeReroll`)をスキップ＝品揃え持ち越し、手動リロールで自動解除。検証:realCS=0、Sceneで ShopBoard sprite=bottom_bar_background/raycast=false・UIShopに IsShopLocked/EnsureShopChrome/ToggleShopLock を確認。Play実挙動は要ビルド確認。コミットはホスト側で。

- 2026-07-03: **R5-P6b ボタンスキン＋プレート＋ShopBoardフォルダ削除**（DESIGN_R5 §6.6）。①Exp/LevelUp・Rerollボタンの背景sprite（ShopBoard由来のLevelUpButton.png/RerollButton.png）をDuelyst `button_confirm.png` に差替＝脱AI＋見た目統一。②`UIShop.EnsureStatusPlate()`で levelText/money 背面に `status_panel` プレートを敷設（offset方式でアンカー非依存, raycast off, 背面）。③無参照化した `Assets/Images/UI/Shop/ShopBoard/`（ShopBoard/LevelUpButton/RerollButton png+meta＋フォルダmeta）を削除。3 guidの無参照をgrep確認済み。※コンパイル検証はSynapticブリッジがアセット再インポート＋ドメインリロードで一時無応答→復帰後にrealCS確認。コミットはホスト側で。

- 2026-07-03: **R5-P7 ショップをDuelyst風「足元ひし形足場」に作り直し**（DESIGN_R5 §7）。参考画像(Duelyst実機)基準でユーザー決定＝カード=ドット絵＋ひし形足場/素材は既存のみ/並び=緩い弧＋下地透明/操作系=参考画像準拠(Phase2)。①足場素材: `reference/duelyst/.../tiles/tile_board`(45°回転でひし形)＋`tile_glow`を `Assets/Resources/UI/Duelyst/tile_platform(.glow)` に取込(guid新規,border0,Resources.Load確認済128²)。②UICard: 角丸枠frame撤去→`FootSquash`(縦つぶし親)＋`FootDiamond`(tile_platform 45°回転,コスト色)＋`FootGlow`をユニット足元に生成(isometricな床タイル風)、スターアップ時は足場強調。調整public: platformSize/platformSquashY/platformFeetNudge。③UIShop: ShopBoard Image非表示(下地透明)、status_panelプレート撤去、`ApplyShopArc()`でShopSlotを谷型の緩い弧に。realCS=0。※足場のサイズ/位置/弧はPlay/スクショで微調整前提。操作系の参考画像準拠配置はPhase2(スクショ後)。コミットはホスト側で。

- 2026-07-03: **R5-P8 操作系を参考画像準拠に再配置＋FIGHT作り直し**（DESIGN_R5 §7.5）。スクショで現状位置採取(Canvas 1920x1080)＋ユーザー確認(配置OK/FIGHT=赤系大)。`UIShop.ApplyControlLayout()`(Awake 1回)で、左下=経済クラスタ(マナ`coin`+`Money`/`levelText`/`expText`/Ex`LevelUpButton`)、右下=`RerollButton`＋`FIGHT`へ。FIGHTは`RestyleFightButton()`でsprite→Duelyst`button_cancel`(赤系)＋旧レガシーText無効化＋TMP「FIGHT」ラベル新設。realCS=0。各座標はPlay/スクショで微調整前提。コミットはホスト側で。

- 2026-07-03: **R5-P9 ショップ重なり解消＋通貨ジェム修正**（DESIGN_R5 §7.6, Playライブ確認）。①通貨ジェム取り残し=`Find("coin")`がスロットの別coinを誤取得→`FindCurrencyGem()`(money兄弟のManaGem)で正取得し左下へ＋**20→44に拡大**。②枠が右寄り/広すぎで操作系と重複=枠は左端アンカーなのにworld中心を誤算→`ApplyShopArc()`を**ワールド差分方式**(Screen.width/2基準, scaleFactor考慮)に修正、5枠を画面中央(470/715/960/1205/1450)へ。③リロールをFIGHTの上に段積み(最右)。ユニット右端と非干渉。Playライブで重なり解消を確認、コード修正済で次回Play自動適用。realCS=0(要Play停止後の再コンパイル確認)。コミットはホスト側で。

- 2026-07-03: **全画面Duelyst化 P1+P2**（DESIGN_CODEX_DUELYST_UI_RELAYOUT）。P1=`GameHudLayout.cs`新規で1920x1080基準の領域(Arena/TopHud/Left/RightDock/BottomShop)＋操作系座標を一元管理。P2=`UIShop`をGameHudLayout参照へ切替、**FIGHTを赤button_cancel→金button_end_turn_mine(Duelystターンボタン)に差替**(ラベル濃紺＋黒縁)。reference/duelyst/app/resources/ui から end_turn/icon_mana/frame_quest/artifact_frame/bracket/button_primary 等をResources/UI/Duelystへコピー。realCS=0、素材ロード確認。残: P3左ドック/P4右ドック/P5上部HUD/P6背景（実画面スクショで調整しつつ継続）。コミットはホスト側で。

- 2026-07-03: **Duelyst実コード調査＋FIGHT堅牢化＋暗半透明パネル方針**。DuelystのゲームHUDは画像枠でなく暗半透明角丸(rgba(1,0,37,0.75))で地を作ると判明→「無理やり感」の主因を特定。①経済パネル/②ItemBench左ドックを暗半透明角丸に統一(EnsureEconomyPanel/ItemBenchCanvasUI)。③FIGHT堅牢化: SceneのFIGHT既定spriteをnotification_challenge(赤)→button_end_turn_mine(金)に変更、`UIShop.StyleFightButton`(public static, レガシーText活用)をGameManager.EnsureFightButtonPresentationからも適用→金ボタン＋FIGHT文字が安定表示(実機確認済)。realCS=0。残: P4右ドック/P5上部HUD/P6背景。

- 2026-07-03: **P3/P4 左右ドックを暗半透明角丸に統一**。P3=ItemBenchCanvasUIのパネル地を透明→濃紺半透明角丸(左サイドがシナジー行と一体化)。P4=UnitStatusPanelの地を暗色→濃紺角丸半透明で色調統一。FIGHT安定表示・経済/左/右ドックが同系の暗半透明で揃った。realCS=0。残: P5上部HUD(概ね既に良好)/P6背景・盤面タイル(不透明度/敵陣ハイライトの調整=要盤面レンダ変更でやや大)。コミットはホスト側で。

- 2026-07-03: **P5/P6完了＝全画面Duelyst化(P1-P6)一巡**。P5=上部HUD(ラウンド/オプション)は既に暗ピル＋整列で良好、変更不要。P6=`GridManager`の配置タイル色を「味方=半透明の白(α×0.5)/敵陣=薄いピンク(1,0.80,0.82,α×0.38)」に落ち着かせ、盤面背景が見えやすく＝Duelyst風の半透明ハイライトに。realCS=0。これで下部ショップ/左右ドック/上部HUD/盤面が同系の暗半透明＋部品素材で統一。ユーザー承認済(P6=落ち着かせる)。要スクショで最終確認。コミットはホスト側で。

- 2026-07-14: **Discord BOT用知識ベースを生成**（Claude/Cowork、「BOT知識ベース_開発セッション指示書.md」準拠）。プロジェクトルートに `SYNAPTIC.md`（12,410字・必須8セクション・コードマップ索引/トリアージ基準/落とし穴24項）と `features.registry.json`（全24機能・can/cannot/related_files/platform=PC）を新規作成。トリアージEnumはDiscord側固定値に準拠（生成対象外）。related_files 全パス実在・JSONスキーマ検証済み。今後は**各開発セッションの最後に両ファイルへ差分反映**する運用（指示書§更新運用）。コミットはホスト側で。
